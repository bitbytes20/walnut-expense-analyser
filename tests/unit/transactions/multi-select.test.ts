import { describe, it } from 'vitest'

/**
 * Wave 0 scaffold — WORKFLOW-01 multi-select range-select pure logic.
 * These stubs will be implemented in Phase 9 Plan 02 when the renderer
 * multi-select feature is built. Tests exercise pure functions extracted
 * from the multi-select logic (not React components).
 */
describe('multi-select pure logic', () => {
  it.todo('single click selects one row and sets anchor')
  it.todo('shift+click with anchor selects range inclusive of both endpoints')
  it.todo('shift+click with anchor below target selects upward range')
  it.todo('shift+click without anchor behaves as single click')
  it.todo('select-all checks all rows on current page')
  it.todo('deselect-all clears all selections')
  it.todo('indeterminate state when some but not all are selected')
})
