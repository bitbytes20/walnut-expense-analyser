---
phase: 09-workflow-polish
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/renderer/features/transactions/TransactionsScreen.tsx
  - src/renderer/features/transactions/TransactionLedgerTable.tsx
  - src/renderer/features/transactions/TransactionBulkActionBar.tsx
  - tests/unit/transactions/multi-select.test.ts
  - tests/unit/transactions-repository.test.ts
autonomous: true
requirements:
  - WORKFLOW-01
  - WORKFLOW-02
  - WORKFLOW-03

must_haves:
  truths:
    - "User can click checkboxes in the leftmost column to select individual transactions"
    - "User can shift+click to select a contiguous range of transactions"
    - "User can click the header checkbox to select all rows on the current page"
    - "Bulk action bar appears when one or more transactions are selected"
    - "User can assign a category to all selected transactions via inline dropdown in the bulk bar"
    - "User can apply a freeform tag to all selected transactions via inline input in the bulk bar"
    - "Selection clears on page navigation and filter change but NOT on sort change"
  artifacts:
    - path: "src/renderer/features/transactions/TransactionBulkActionBar.tsx"
      provides: "Sticky bulk action bar with category dropdown and tag input"
      min_lines: 80
    - path: "src/renderer/features/transactions/TransactionLedgerTable.tsx"
      provides: "Checkbox column with indeterminate header state"
      contains: "selectAllRef"
    - path: "src/renderer/features/transactions/TransactionsScreen.tsx"
      provides: "Multi-select state management, shift anchor ref, selection reset on page/filter change"
      contains: "selectedIds"
  key_links:
    - from: "src/renderer/features/transactions/TransactionsScreen.tsx"
      to: "src/renderer/features/transactions/TransactionLedgerTable.tsx"
      via: "selectedIds, onSelect, onSelectAll props"
      pattern: "selectedIds.*onSelect"
    - from: "src/renderer/features/transactions/TransactionBulkActionBar.tsx"
      to: "window.walnut.bulkUpdateTransactions"
      via: "IPC call for bulk category/tag update"
      pattern: "bulkUpdateTransactions"
---

<objective>
Implement transaction multi-select with checkboxes, shift+click range-select, and bulk category/tag assignment via a sticky action bar.

Purpose: Users can select multiple transactions and apply a category or tag to all of them in one step, eliminating repetitive one-by-one editing (WORKFLOW-01, WORKFLOW-02, WORKFLOW-03 per D-01 through D-05).
Output: Working multi-select UI in the transaction ledger with bulk category and tag assignment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-workflow-polish/09-CONTEXT.md
@.planning/phases/09-workflow-polish/09-RESEARCH.md
@.planning/phases/09-workflow-polish/09-UI-SPEC.md
@.planning/phases/09-workflow-polish/09-01-SUMMARY.md

<interfaces>
<!-- Contracts defined in Plan 01 -->

From src/shared/contracts/transactions.ts:
```typescript
export const BulkUpdateTransactionsInputSchema = z.object({
  transactionIds: z.array(z.string()).min(1),
  categoryId: z.string().nullable().optional(),
  category: z.string().nullable().optional(),
  tags: z.array(z.string()).optional()
})
export type BulkUpdateTransactionsInput = z.infer<typeof BulkUpdateTransactionsInputSchema>
export type BulkUpdateTransactionsResult = { updatedCount: number }
```

From src/shared/contracts/app-state.ts (WalnutApi):
```typescript
bulkUpdateTransactions: (input: BulkUpdateTransactionsInput) => Promise<BulkUpdateTransactionsResult>
```

Existing rendering patterns from src/renderer/features/import/ReviewBulkActionBar.tsx:
- Sticky bar with frosted surface: `background: 'rgba(245, 241, 232, 0.96)'`, `boxShadow: 'var(--shadow-panel)'`
- Kicker label: `fontSize: 12, fontWeight: 600, color: 'var(--color-accent)'`, text: `"Bulk actions"`
- Heading: `fontSize: 20, fontWeight: 600` for count
- Button pattern: `minHeight: 44`, pill shape with border radius
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Multi-select state and checkbox column</name>
  <files>
    src/renderer/features/transactions/TransactionsScreen.tsx,
    src/renderer/features/transactions/TransactionLedgerTable.tsx,
    tests/unit/transactions/multi-select.test.ts
  </files>
  <read_first>
    src/renderer/features/transactions/TransactionsScreen.tsx,
    src/renderer/features/transactions/TransactionLedgerTable.tsx,
    src/renderer/features/import/ReviewBulkActionBar.tsx,
    tests/unit/transactions/multi-select.test.ts,
    .planning/phases/09-workflow-polish/09-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: single click on unchecked row adds its ID to selectedIds and sets anchor
    - Test: single click on checked row removes its ID from selectedIds and clears anchor
    - Test: shift+click with anchor selects all IDs between anchor and target (inclusive), preserving existing selections outside range
    - Test: shift+click with anchor below target selects upward range correctly
    - Test: shift+click without anchor behaves as single click
    - Test: selectAll(true) selects all pagedRow IDs; selectAll(false) clears all
    - Test: indeterminate state is true when some but not all rows are selected
  </behavior>
  <action>
**Extract pure multi-select logic** into a testable module. Create helper functions (not React hooks — pure functions) that the tests and component can both use:

```typescript
// In TransactionsScreen.tsx or a shared util:
export function computeRangeSelect(
  pagedRowIds: string[],
  anchorId: string | null,
  targetId: string,
  checked: boolean,
  currentSelection: Set<string>
): { selectedIds: Set<string>; newAnchor: string | null }
```

**Implement tests first** in `tests/unit/transactions/multi-select.test.ts` — replace the `.todo` stubs from Plan 01 with real test implementations that call the pure functions.

**Then implement in TransactionsScreen.tsx:**
- Add `const [selectedIds, setSelectedIds] = useState<Set<string>>(new Set())`
- Add `const shiftAnchorRef = useRef<string | null>(null)`
- Add `useEffect` that resets selection when `currentPage`, `submittedSearch`, or filter state changes. Do NOT include sort columns in the dependency array (per D-02 intent: selection persists across sort changes within the same page).
- Add `handleRowSelect(id: string, checked: boolean, shiftHeld: boolean)` using the pure logic function.
- Add `handleSelectAll(checked: boolean)` — selects/deselects all `pagedRows` IDs.
- Pass `selectedIds`, `onSelect`, `onSelectAll` as props to `TransactionLedgerTable`.

**Then implement in TransactionLedgerTable.tsx:**
- Accept new props: `selectedIds: Set<string>`, `onSelect: (id: string, checked: boolean, shiftHeld: boolean) => void`, `onSelectAll: (checked: boolean) => void`
- Add a leftmost `<th>` with width `40px` containing an `<input type="checkbox" ref={selectAllRef} aria-label="Select all on this page" />`.
- Use `useRef<HTMLInputElement>` + `useEffect` to set `ref.current.indeterminate` when some but not all rows are selected (React does not support declarative `indeterminate`).
- Add a leftmost `<td>` per row with `<input type="checkbox" checked={selectedIds.has(row.id)} aria-label={"Select " + row.description} onChange={...} />`.
- In the row checkbox onChange, detect shift key via `(e.nativeEvent as MouseEvent).shiftKey` and pass to `onSelect`.
- Range-select visual feedback: rows within shift-select range get `background: rgba(15, 118, 110, 0.06)` tint (per UI-SPEC).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/transactions/multi-select.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/transactions/multi-select.test.ts has at least 7 passing tests (not todo/skip)
    - TransactionsScreen.tsx contains `const [selectedIds, setSelectedIds] = useState`
    - TransactionsScreen.tsx contains `shiftAnchorRef`
    - TransactionsScreen.tsx contains `useEffect` that calls `setSelectedIds(new Set())` with page/filter dependencies
    - TransactionLedgerTable.tsx contains `selectAllRef` and `ref.current.indeterminate`
    - TransactionLedgerTable.tsx contains `aria-label="Select all on this page"`
    - TransactionLedgerTable.tsx contains checkbox `<input type="checkbox"` per row
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Multi-select state, checkbox column, shift+click range-select, and select-all are working. Selection resets on page/filter change. All multi-select unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Bulk action bar with category dropdown and tag input</name>
  <files>
    src/renderer/features/transactions/TransactionBulkActionBar.tsx,
    src/renderer/features/transactions/TransactionsScreen.tsx,
    tests/unit/transactions-repository.test.ts
  </files>
  <read_first>
    src/renderer/features/transactions/TransactionsScreen.tsx,
    src/renderer/features/import/ReviewBulkActionBar.tsx,
    tests/unit/transactions-repository.test.ts,
    .planning/phases/09-workflow-polish/09-UI-SPEC.md
  </read_first>
  <action>
**Create `TransactionBulkActionBar.tsx`** — a new component following the `ReviewBulkActionBar` visual pattern.

Props:
```typescript
interface TransactionBulkActionBarProps {
  selectedCount: number
  categories: CategoryTreeNode[]
  onAssignCategory: (categoryId: string, categoryLabel: string) => Promise<void>
  onApplyTag: (tag: string) => Promise<void>
  onClearSelection: () => void
}
```

Layout (per UI-SPEC and D-03, D-04, D-05):
- Container: `position: 'sticky', top: 0, zIndex: 10, display: 'grid', gap: 'var(--space-md)', padding: 'var(--space-md) var(--space-lg)', borderRadius: 'var(--radius-lg)', border: '1px solid var(--color-border)', background: 'rgba(245, 241, 232, 0.96)', boxShadow: 'var(--shadow-panel)'`
- Row 1: Kicker label `"Bulk actions"` — `fontSize: 12, fontWeight: 600, color: 'var(--color-accent)'`
- Row 2: Heading `"{N} selected"` — `fontSize: 20, fontWeight: 600`
- Row 3: Action controls inline — category dropdown + tag input + clear selection button
- Category dropdown: `<select>` or custom listbox with `minHeight: 44`. Options from `categories` prop. Label: `"Assign category"`. On change, calls `onAssignCategory(selectedCategoryId, selectedCategoryLabel)`.
- Tag input: text input with `minHeight: 44, placeholder: "Add tag to selected"` + "Apply tag" button. On submit, calls `onApplyTag(tagValue)`.
- `role="region"` and `aria-label="Bulk actions"` on container.
- Confirmation messages rendered in an `aria-live="polite"` region: `"{N} transactions updated."` for category, `"Tag applied to {N} transactions."` for tag. Show for 3 seconds then clear.

**Wire in TransactionsScreen.tsx:**
- Import and render `TransactionBulkActionBar` conditionally when `selectedIds.size > 0`, positioned between pagination/header area and the table.
- `onAssignCategory` calls `window.walnut.bulkUpdateTransactions({ transactionIds: [...selectedIds], categoryId, category })`. On success, refresh the transaction list (re-fetch) and show confirmation count in the bar.
- `onApplyTag` calls `window.walnut.bulkUpdateTransactions({ transactionIds: [...selectedIds], tags: [tag] })`. On success, refresh and show confirmation.
- `onClearSelection` calls `setSelectedIds(new Set())`.
- After a successful bulk operation, clear selection and re-fetch the transaction list.

**Extend repository tests** in `tests/unit/transactions-repository.test.ts`:
- Add test: `bulkUpdateTransactions with categoryId updates all specified rows and returns correct count`
- Add test: `bulkUpdateTransactions with tags updates tags_json on all specified rows`
- Add test: `bulkUpdateTransactions with empty transactionIds array is rejected by schema validation`
- Add test: `bulkUpdateTransactions with nonexistent IDs returns updatedCount 0`
  </action>
  <verify>
    <automated>npx vitest run tests/unit/transactions-repository.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/features/transactions/TransactionBulkActionBar.tsx exists with at least 80 lines
    - TransactionBulkActionBar.tsx contains `role="region"`
    - TransactionBulkActionBar.tsx contains `aria-label="Bulk actions"`
    - TransactionBulkActionBar.tsx contains `aria-live="polite"`
    - TransactionBulkActionBar.tsx contains `Assign category`
    - TransactionBulkActionBar.tsx contains `Add tag to selected`
    - TransactionBulkActionBar.tsx contains `Apply tag`
    - TransactionBulkActionBar.tsx contains `transactions updated`
    - TransactionBulkActionBar.tsx contains `Tag applied to`
    - TransactionsScreen.tsx contains `TransactionBulkActionBar`
    - TransactionsScreen.tsx contains `bulkUpdateTransactions`
    - transactions-repository.test.ts contains `bulkUpdateTransactions`
    - All tests in transactions-repository.test.ts pass
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Bulk action bar renders with category dropdown and tag input. Bulk IPC calls work end-to-end. Confirmation messages display. Repository tests for bulk update pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/unit/transactions` passes — multi-select and repository tests green
2. `npx tsc --noEmit` exits 0
3. Checkbox column visible in table with indeterminate header state
4. Bulk action bar appears/disappears based on selection
5. Bulk category and tag assignment work through IPC
</verification>

<success_criteria>
- Checkboxes in leftmost column of transaction table (D-01)
- Select-all covers current page only (D-02)
- Shift+click range-select working
- Bulk action bar sticky below header when selected (D-03)
- Inline category dropdown in bar (D-04)
- Inline tag input in bar (D-05)
- Selection resets on page/filter change, persists on sort
- Confirmation messages show count
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-workflow-polish/09-02-SUMMARY.md`
</output>
