---
wave: 3
depends_on: ["07-01-PLAN.md", "07-02-PLAN.md", "07-03-PLAN.md"]
files_modified: ["src/renderer/features/audit/AuditScreen.tsx", "src/renderer/App.tsx", "src/renderer/features/settings/SettingsScreen.tsx"]
autonomous: true
requirements_addressed: ["D-01", "D-04", "AUDT-01"]
---

# Plan 04: Global Audit Workspace & Settings Panel

## Objective
Provide the graphical interfaces for traversing the audit ledger and exporting the diagnostics bundles.

## Overview
This UI work integrates with the backend APIs established in Wave 1 and Wave 2. It requires following the spacing, typography, and color contracts locked in `07-UI-SPEC.md`.

## Tasks

<task>
<read_first>
- `src/renderer/App.tsx`
- `src/renderer/features/audit/AuditScreen.tsx` (create new)
- `.planning/phases/07-audit-and-diagnostics/07-UI-SPEC.md`
</read_first>
<action>
Create `AuditScreen.tsx` in a new feature directory. Connect it to the `App.tsx` routing replacing the existing `Audit workspace placeholder` disabled button with a real nav item. Use `getAuditEvents()` to fetch the ledger.
</action>
<acceptance_criteria>
- `App.tsx` imports and renders `AuditScreen`.
- The placeholder disabled attribute is removed.
</acceptance_criteria>
</task>

<task>
<read_first>
- `src/renderer/features/audit/AuditScreen.tsx`
- `.planning/phases/07-audit-and-diagnostics/07-UI-SPEC.md`
</read_first>
<action>
Implement the Filtered chron-feed UI in `AuditScreen.tsx`. Render a single list of events chronologically. Above the list, render category toggle buttons (e.g. `[x] Security`, `[x] Edits`, `[x] Review`). Apply the active state `var(--accent-blue)` for active toggles. As mandated by `07-UI-SPEC.md`, use the `Body 14px` styles for descriptions, `Label 12px` for timestamps, and a `No events found` empty state if the resulting query is empty.
</action>
<acceptance_criteria>
- The screen renders an active filter bar.
- Empty states match Phase 7 copy contract.
</acceptance_criteria>
</task>

<task>
<read_first>
- `src/renderer/features/settings/SettingsScreen.tsx`
</read_first>
<action>
Extend the `Settings` space (if one exists, otherwise scaffold one) with a new "Support" section. Add two primary actions: "Export Redacted Diagnostics" and "Export Full Diagnostics" which trigger the `generateDiagnosticsBundle` IPC method and prompt a file-save dialog to dump the text. Add a section mentioning that crash logs are silently retained in the `{userData}/logs/walnut.log` folder (D-04).
</action>
<acceptance_criteria>
- Settings screen contains export buttons for the diagnostics.
- The UI clarifies the location of crash logs without requiring them to be surfaced on startup.
</acceptance_criteria>
</task>
