---
phase: 08-settings-and-release-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/renderer/features/settings/SettingsScreen.tsx
autonomous: false
requirements:
  - SETG-01
  - SECU-03
  - ACCS-01
user_setup: []
must_haves:
  truths:
    - "Settings screen has six sections in order: Preferences, Security, Backup, Support, Feature Flags (Lab), Danger Zone"
    - "Theme toggle switches between Light/Dark/System and persists immediately"
    - "Idle-lock timeout selector offers 5min/15min/30min/Never and persists immediately"
    - "Change PIN card validates current PIN, new PIN 6+ digits, confirmation match"
    - "Create Backup uses native OS save dialog and produces .wbk file"
    - "Restore from Backup uses native OS open dialog, prompts for PIN, and routes to PIN setup on success"
    - "Lab section has AI summaries toggle, off by default, persisted"
    - "Clear Transactions shows single confirmation dialog and deletes transactions + associated audit events"
    - "Full App Reset requires typing RESET to confirm, then wipes everything and routes to onboarding"
    - "All interactive elements are keyboard focusable with visible focus rings"
  artifacts:
    - path: "src/renderer/features/settings/SettingsScreen.tsx"
      provides: "Complete settings screen with all six sections"
      contains: "Preferences"
  key_links:
    - from: "src/renderer/features/settings/SettingsScreen.tsx"
      to: "window.walnut.getAppConfig"
      via: "IPC call for loading current config"
      pattern: "getAppConfig"
    - from: "src/renderer/features/settings/SettingsScreen.tsx"
      to: "window.walnut.setAppConfig"
      via: "IPC call for persisting config changes"
      pattern: "setAppConfig"
    - from: "src/renderer/features/settings/SettingsScreen.tsx"
      to: "window.walnut.exportBackup"
      via: "Create Backup action"
      pattern: "exportBackup"
    - from: "src/renderer/features/settings/SettingsScreen.tsx"
      to: "window.walnut.importBackup"
      via: "Restore from Backup action"
      pattern: "importBackup"
    - from: "src/renderer/features/settings/SettingsScreen.tsx"
      to: "window.walnut.changePin"
      via: "Change PIN form submission"
      pattern: "changePin"
    - from: "src/renderer/features/settings/SettingsScreen.tsx"
      to: "window.walnut.clearTransactions"
      via: "Clear Transactions danger action"
      pattern: "clearTransactions"
    - from: "src/renderer/features/settings/SettingsScreen.tsx"
      to: "window.walnut.fullReset"
      via: "Full App Reset danger action"
      pattern: "fullReset"
---

<objective>
Expand the Settings screen from a single Support section into the full six-section layout with all interactive controls wired to the IPC backend.

Purpose: Deliver the complete owner settings experience per SETG-01 and SECU-03 — the final UI surface for Phase 8. This is the single largest renderer task in the phase and touches only one file.
Output: Fully functional Settings screen with Preferences, Security, Backup, Support, Feature Flags (Lab), and Danger Zone sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-settings-and-release-hardening/08-CONTEXT.md
@.planning/phases/08-settings-and-release-hardening/08-RESEARCH.md
@.planning/phases/08-settings-and-release-hardening/08-UI-SPEC.md
@.planning/phases/08-settings-and-release-hardening/08-VALIDATION.md
@.planning/phases/08-settings-and-release-hardening/08-01-SUMMARY.md
@.planning/phases/08-settings-and-release-hardening/08-02-SUMMARY.md

<interfaces>
<!-- IPC methods available from Plan 01 and Plan 02. -->

From WalnutApi (after Plan 01 + 02):
```typescript
// Config
getAppConfig: () => Promise<AppConfig>
setAppConfig: (input: Partial<AppConfig>) => Promise<AppConfig>

// Security
changePin: (input: ChangePinInput) => Promise<{ ok: boolean; error?: string }>

// Backup
exportBackup: (pin: string) => Promise<BackupResult>
importBackup: (pin: string) => Promise<RestoreResult>

// Cleanup
clearTransactions: () => Promise<ClearTransactionsResult>
fullReset: () => Promise<AppShellState>

// Existing
generateDiagnosticsBundle: (input: { type: 'redacted' | 'full' }) => Promise<string>
```

From AppConfig type:
```typescript
interface AppConfig {
  theme: 'light' | 'dark' | 'system'
  idleLockTimeoutMs: number  // 300000 | 900000 | 1800000 | 0
  featureFlags: { aiSummaries: boolean }
}
```

From BackupResult/RestoreResult:
```typescript
interface BackupResult { ok: boolean; filePath?: string; error?: string }
interface RestoreResult { ok: boolean; householdName?: string; error?: string }
interface ChangePinInput { currentPin: string; newPin: string }
interface ClearTransactionsResult { deletedCount: number }
```

Existing SettingsScreen.tsx patterns (to preserve):
- inline style objects with CSS variables
- actionCard pattern (flex row, content left, button right)
- section with aria-labelledby and h2 heading
- ExportState type for async button states
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Preferences + Security + Backup sections</name>
  <files>src/renderer/features/settings/SettingsScreen.tsx</files>
  <read_first>
    src/renderer/features/settings/SettingsScreen.tsx,
    src/renderer/features/lock-screen/LockScreen.tsx,
    src/shared/contracts/app-state.ts,
    src/renderer/styles/tokens.css
  </read_first>
  <action>
    **Rewrite SettingsScreen.tsx** to contain the full six-section layout. The existing Support section code is preserved and repositioned to section 4.

    **Page header** (keep existing):
    - Title: "Settings" (24px, 600)
    - Subtitle: "Configure preferences, security, backup, and support tools." (updated copy per UI-SPEC)

    **Section 1: Preferences** (D-01, D-02)

    Section heading: "Preferences" with `<section aria-labelledby="prefs-heading">`

    **Theme toggle** — segmented button group with 3 options: Light / Dark / System
    - Load initial value from `window.walnut.getAppConfig()` on mount
    - Active option: `background: var(--color-accent)`, `color: #fff`
    - Inactive option: `background: var(--color-surface)`, `color: var(--color-ink)`, `border: 1px solid var(--color-border)`
    - On click: call `window.walnut.setAppConfig({ theme: value })`, then call `applyTheme(value)`:
      ```typescript
      function applyTheme(theme: 'light' | 'dark' | 'system') {
        const html = document.documentElement
        if (theme === 'light') html.setAttribute('data-theme', 'light')
        else if (theme === 'dark') html.setAttribute('data-theme', 'dark')
        else html.removeAttribute('data-theme')
      }
      ```
    - Each segment: `<button>` with `aria-pressed={isActive}`

    **Idle-lock timeout** — `<select>` control
    - Label: "Lock after inactivity"
    - Options: `[{ label: '5 minutes', value: 300000 }, { label: '15 minutes', value: 900000 }, { label: '30 minutes', value: 1800000 }, { label: 'Never', value: 0 }]`
    - Default: 900000 (15 minutes) — loaded from AppConfig
    - Helper text below: "The app will lock itself after this period of keyboard and mouse inactivity."
    - On change: `window.walnut.setAppConfig({ idleLockTimeoutMs: Number(value) })`
    - Select styling: `padding: 8px 16px`, `borderRadius: 6`, `border: 1px solid var(--color-border)`, `background: var(--color-surface)`, `color: var(--color-ink)`, `fontSize: 14`

    **Section 2: Security** (D-14 for confirmation UX)

    Section heading: "Security" with `<section aria-labelledby="security-heading">`

    **Change PIN** — action card with expandable inline form
    - Default state: action card with label "Change PIN", description "Update your 6+ digit numeric PIN.", button "Change PIN" (secondary styling)
    - On click: expand an inline card below with three inputs:
      - "Current PIN" — `type="password"`, `inputMode="numeric"`, `pattern="[0-9]*"`, `minLength={6}`
      - "New PIN" — same attributes
      - "Confirm New PIN" — same attributes
    - Validation:
      - New PIN must be 6+ digits
      - Confirm must match New PIN
      - Show error text in `var(--color-destructive)` 12px below mismatched field:
        - Mismatch: "New PIN and confirmation do not match."
        - Too short: "PIN must be at least 6 digits."
    - CTA: "Update PIN" — accent primary, `disabled` until all valid
    - On submit: `const result = await window.walnut.changePin({ currentPin, newPin })`
      - If `result.ok`: collapse form, show "PIN updated successfully" for 3 seconds
      - If `!result.ok`: show `result.error` as inline error below Current PIN field ("Current PIN is incorrect.")
    - Keyboard: Ctrl+S submits when card is open (per D-10)

    **Recovery key note** — small note card below Change PIN:
    - "Your recovery key was created during onboarding. If you lose your PIN, use the recovery key on the lock screen to reset it."

    **Section 3: Backup** (D-04, D-05, D-06)

    Section heading: "Backup" with `<section aria-labelledby="backup-heading">`

    Two action cards (reuse `actionCard` style):

    1. **Create Backup**
       - Label: "Create Backup"
       - Description: "Saves an AES-256 encrypted backup of your data. Use your current PIN to decrypt it."
       - Button: "Create Backup" — accent primary styling
       - On click: show inline PIN input ("Enter your current PIN to encrypt the backup"), then call `window.walnut.exportBackup(pin)`
       - Loading state: button text "Creating backup...", disabled
       - Success: "Backup saved." for 3 seconds
       - Error: "Backup failed. Check available disk space and try again."

    2. **Restore from Backup**
       - Label: "Restore from Backup"
       - Description: "Import a .wbk backup file. You will need the PIN used when the backup was created."
       - Button: "Restore from Backup" — secondary styling
       - On click: the importBackup IPC shows the file dialog internally. The renderer flow is: (1) show inline PIN input, (2) user enters PIN and clicks "Restore", (3) call `window.walnut.importBackup(pin)`, (4) IPC shows native open dialog, (5) returns result.
       - Success: "Backup restored. Set a new PIN to continue." — then trigger navigation to PIN setup (call the parent's `onRequirePinSetup` callback or route to onboarding pin-setup step)
       - Error: "Wrong PIN or corrupted backup file. Check the file and try again."

    **Backup warning note** — `role="note"` panel below both cards:
    - "Backup files contain all your transaction data. They are owner-only and not safe to share externally. Use the redacted diagnostics bundle for support requests."
    - Styled: `var(--color-surface)` background, `var(--color-border)` border, `borderLeft: 3px solid rgba(180, 35, 24, 0.20)` amber-warning tint

    **Section separator** — `borderTop: '1px solid var(--color-border)'`, `paddingTop: var(--space-xl)` between each section.

    State management: use `useState` for each form state — `changePinExpanded`, `changePinState` (idle/submitting/success/error), `backupState`, `restoreState`, `restorePinInput`, etc. Follow existing ExportState pattern.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/features/settings/SettingsScreen.tsx contains `aria-labelledby="prefs-heading"`
    - src/renderer/features/settings/SettingsScreen.tsx contains `aria-labelledby="security-heading"`
    - src/renderer/features/settings/SettingsScreen.tsx contains `aria-labelledby="backup-heading"`
    - src/renderer/features/settings/SettingsScreen.tsx contains `applyTheme`
    - src/renderer/features/settings/SettingsScreen.tsx contains `data-theme`
    - src/renderer/features/settings/SettingsScreen.tsx contains `setAppConfig`
    - src/renderer/features/settings/SettingsScreen.tsx contains `idleLockTimeoutMs`
    - src/renderer/features/settings/SettingsScreen.tsx contains `changePin`
    - src/renderer/features/settings/SettingsScreen.tsx contains `exportBackup`
    - src/renderer/features/settings/SettingsScreen.tsx contains `importBackup`
    - src/renderer/features/settings/SettingsScreen.tsx contains `aria-pressed`
    - src/renderer/features/settings/SettingsScreen.tsx contains `Lock after inactivity`
    - src/renderer/features/settings/SettingsScreen.tsx contains `PIN updated successfully`
    - src/renderer/features/settings/SettingsScreen.tsx contains `.wbk`
    - TypeScript compilation produces no errors
  </acceptance_criteria>
  <done>
    Preferences section has working theme toggle (Light/Dark/System) and idle-lock timeout selector. Security section has expandable Change PIN form with validation. Backup section has Create/Restore action cards with PIN prompts, native file dialogs, and loading/success/error states.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Feature Flags (Lab) + Danger Zone sections + accessibility polish</name>
  <files>src/renderer/features/settings/SettingsScreen.tsx</files>
  <read_first>
    src/renderer/features/settings/SettingsScreen.tsx,
    src/shared/contracts/app-state.ts
  </read_first>
  <action>
    Continue expanding SettingsScreen.tsx. The Support section (existing code from Phase 7) should be Section 4, positioned after Backup and before Lab.

    **Section 4: Support** (existing — repositioned)
    Keep the existing diagnostics export cards and crash log note exactly as they are. Just ensure the section has proper `aria-labelledby="support-heading"` (it already does).

    **Section 5: Feature Flags / Lab** (D-03, D-11, D-12)

    Section heading: "Lab" with `<section aria-labelledby="lab-heading">`

    Section intro note: "Features in Lab are experimental and may change in future releases." (12px, `var(--color-muted)`)

    **AI Summaries toggle row:**
    - Layout: flex row, label left, toggle right
    - Label: "AI Summaries" (14px, 400)
    - Description below label: "Show a narrative AI summary on the dashboard. Off by default. Requires an external AI service when enabled." (12px, 400, `var(--color-muted)`)
    - Toggle: custom pill-shaped CSS toggle switch
      - `role="switch"`, `aria-checked={checked}`, `tabIndex={0}`
      - Track: 40px wide, 22px tall, `borderRadius: 11px`
      - Checked: `background: var(--color-accent)` track
      - Unchecked: `background: var(--color-border)` track
      - Thumb: 18px circle, `background: #fff`, positioned with `transform: translateX(${checked ? 20 : 2}px)`, `transition: transform 0.15s`
      - Keyboard: Space/Enter toggles
      - On toggle: `window.walnut.setAppConfig({ featureFlags: { aiSummaries: !current } })`
    - Initial value: loaded from AppConfig on mount (off by default)

    **Section 6: Danger Zone** (D-13, D-14)

    Section container:
    - `borderLeft: '3px solid var(--color-destructive)'`
    - `paddingLeft: var(--space-md)`
    - `background: rgba(180, 35, 24, 0.04)` tint
    - `borderRadius: 8px`
    - `padding: var(--space-lg)` (all sides)

    Section heading: "Danger Zone" with `<section aria-labelledby="danger-heading">`

    **Card 1: Clear Transactions**
    - Description: "Permanently deletes all transaction records and their associated audit events. Your categories, rules, and account profile are kept."
    - Button: "Clear Transactions" — destructive styling (`background: var(--color-destructive)`, `color: #fff`)
    - On click: show confirmation modal overlay (D-14 single dialog):
      - Modal: `position: fixed`, `inset: 0`, backdrop `background: rgba(0,0,0,0.45)`, centered flex
      - Content box: `background: var(--color-surface)`, `borderRadius: var(--radius-lg, 12px)`, `boxShadow: var(--shadow-panel, 0 4px 24px rgba(0,0,0,0.12))`, `padding: var(--space-xl)`, `maxWidth: 420px`
      - `aria-modal="true"`, `aria-labelledby="clear-txn-title"`
      - Heading: "Clear all transactions?"
      - Body: "This cannot be undone. Your categories, rules, and profile will not be affected."
      - Buttons: "Keep Transactions" (secondary) / "Clear Transactions" (destructive)
      - Focus trap: auto-focus on "Keep Transactions" (safe default)
      - Escape closes modal
      - On confirm: `const result = await window.walnut.clearTransactions()`; show brief success "Cleared {result.deletedCount} transactions." for 3 seconds; close modal

    **Card 2: Full App Reset**
    - Description: "Wipes everything and returns the app to first-run onboarding. This cannot be undone."
    - Button: "Reset App" — destructive styling
    - On click: show type-to-confirm modal:
      - Same modal chrome as above
      - `aria-modal="true"`, `aria-labelledby="reset-title"`
      - Heading: "Full App Reset"
      - Instruction: "Type RESET to permanently wipe all data and return to onboarding."
      - `<input>` — controlled, `aria-label="Type RESET to confirm"`, `aria-describedby="reset-instruction"`
      - "Reset App" confirm button: destructive, `disabled` until input value exactly equals `RESET`
      - "Cancel" button: secondary
      - Escape closes modal
      - On confirm: `const state = await window.walnut.fullReset()`; parent callback to navigate to onboarding (the SettingsScreen needs a prop like `onFullReset` that the parent App.tsx provides — or dispatch state update via the returned AppShellState)

    **Accessibility polish across all sections** (ACCS-01):
    - All buttons, selects, toggle have visible focus ring: add `:focus-visible` style or inline `outline: '2px solid var(--color-accent)'`, `outlineOffset: '2px'` on focus
    - Modals trap focus (tab cycles within modal content only)
    - Success messages use `aria-live="polite"` for screen reader announcement
    - All `<section>` elements have `aria-labelledby` pointing to their heading `id`
    - Toggle switch has `role="switch"` and `aria-checked`
    - Segmented theme buttons have `aria-pressed`
    - Loading button states update label (e.g., "Creating backup..." not just disabled)

    **Props update:** The SettingsScreen component needs to accept callbacks for post-restore and post-reset navigation. Add props:
    ```typescript
    interface SettingsScreenProps {
      onRequirePinSetup?: () => void  // after backup restore
      onFullReset?: (state: AppShellState) => void  // after full reset
    }
    ```
    Wire these in `App.tsx` where `<SettingsScreen>` is rendered — `onRequirePinSetup` should navigate to onboarding PIN setup step, `onFullReset` should set app state to the returned onboarding state.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/features/settings/SettingsScreen.tsx contains `aria-labelledby="lab-heading"`
    - src/renderer/features/settings/SettingsScreen.tsx contains `aria-labelledby="danger-heading"`
    - src/renderer/features/settings/SettingsScreen.tsx contains `role="switch"`
    - src/renderer/features/settings/SettingsScreen.tsx contains `aria-checked`
    - src/renderer/features/settings/SettingsScreen.tsx contains `AI Summaries`
    - src/renderer/features/settings/SettingsScreen.tsx contains `aiSummaries`
    - src/renderer/features/settings/SettingsScreen.tsx contains `clearTransactions`
    - src/renderer/features/settings/SettingsScreen.tsx contains `fullReset`
    - src/renderer/features/settings/SettingsScreen.tsx contains `Type RESET`
    - src/renderer/features/settings/SettingsScreen.tsx contains `aria-modal`
    - src/renderer/features/settings/SettingsScreen.tsx contains `aria-live="polite"`
    - src/renderer/features/settings/SettingsScreen.tsx contains `Danger Zone`
    - src/renderer/features/settings/SettingsScreen.tsx contains `rgba(180, 35, 24, 0.04)`
    - src/renderer/features/settings/SettingsScreen.tsx contains `onFullReset` or `onRequirePinSetup`
    - TypeScript compilation produces no errors
  </acceptance_criteria>
  <done>
    Lab section has AI Summaries toggle with role="switch" and persistence. Danger Zone has Clear Transactions (single confirm dialog) and Full Reset (type-to-confirm modal). All sections have proper aria attributes, focus management, and keyboard support. SettingsScreen accepts navigation callbacks for post-restore and post-reset flows.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Settings screen and keyboard shortcuts</name>
  <files>src/renderer/features/settings/SettingsScreen.tsx</files>
  <action>
    Human verification of the complete Phase 8 deliverable. Run `npm start` and walk through the 15-step verification checklist below. No code changes expected — this is a visual and functional review checkpoint.
  </action>
  <verify>
    <automated>npm run test:unit -- --reporter=dot</automated>
  </verify>
  <done>Owner approves the Settings screen layout, all six sections, keyboard shortcuts, and interactive flows.</done>
  <what-built>Complete Settings screen with all six sections: Preferences (theme toggle, idle timeout), Security (change PIN), Backup (create/restore with encryption), Support (diagnostics export), Lab (AI summaries toggle), and Danger Zone (clear transactions, full reset). Plus keyboard shortcuts Ctrl+1-8 for workspace navigation.</what-built>
  <how-to-verify>
    1. Run the app: `npm start`
    2. Navigate to Settings (Ctrl+7 should work)
    3. Verify six sections appear in order: Preferences, Security, Backup, Support, Lab, Danger Zone
    4. Toggle theme between Light/Dark/System — verify colors change immediately and persist after reload
    5. Change idle lock timeout — verify the select has 4 options (5min, 15min, 30min, Never)
    6. Expand "Change PIN" — verify three inputs appear, try with wrong current PIN (should show error), try with correct PIN and mismatched confirm (should show error), try with valid inputs (should show success)
    7. Test "Create Backup" — should open native save dialog, produce .wbk file
    8. Test "Restore from Backup" — should prompt for PIN then open native open dialog
    9. Toggle AI Summaries in Lab section — verify it persists after reload
    10. Test "Clear Transactions" — verify single confirmation dialog appears
    11. Test "Full App Reset" — verify type-to-confirm modal appears, button is disabled until RESET is typed
    12. Test Ctrl+1 through Ctrl+6 navigation — verify correct workspace opens
    13. Test Ctrl+8 — verify app locks
    14. Verify Tab navigation works through all interactive elements with visible focus rings
    15. Test typing in a search input then pressing Ctrl+1 — verify it does NOT navigate away (input guard)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run test:unit` full suite passes
- Settings screen renders all six sections with correct ordering
- Theme toggle, idle timeout, change PIN, backup/restore, feature flags, and danger zone actions all work end-to-end
- Keyboard shortcuts verified manually
</verification>

<success_criteria>
1. Settings screen has all six sections in correct order per D-02
2. Theme toggle persists across app reload
3. Idle-lock timeout has 4 options with 15 minutes as default
4. Change PIN validates current PIN and enforces 6+ digit new PIN
5. Backup creates .wbk file encrypted with AES-256-GCM via PIN-derived key
6. Restore decrypts with provided PIN and routes to fresh PIN setup
7. AI Summaries toggle is off by default and persists in app config
8. Clear Transactions shows single confirmation dialog
9. Full Reset requires typing RESET before confirming
10. Danger Zone is visually distinct with destructive styling
11. All interactive elements are keyboard accessible with visible focus rings
</success_criteria>

<output>
After completion, create `.planning/phases/08-settings-and-release-hardening/08-04-SUMMARY.md`
</output>
