---
phase: 01-product-shell-and-security
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/main/security/pin-service.ts
  - src/main/security/session-lock.ts
  - src/main/ipc/security.ts
  - src/renderer/features/lock-screen/LockScreen.tsx
  - src/renderer/features/lock-screen/RecoveryResetFlow.tsx
  - src/renderer/features/app-shell/AppShell.tsx
  - src/shared/contracts/security.ts
  - tests/unit/pin-service.test.ts
  - tests/e2e/lock-screen.spec.ts
autonomous: true
requirements:
  - SECU-01
  - SECU-02
must_haves:
  truths:
    - "The app requires a valid PIN on launch and after idle or OS-session lock conditions."
    - "Failed PIN attempts trigger exponential backoff messaging instead of a permanent lockout."
    - "Recovery-key reset rotates the recovery key, requires a new PIN, and records a visible security event."
  artifacts:
    - path: "src/renderer/features/lock-screen/LockScreen.tsx"
      provides: "Branded unlock UI with account context and tertiary recovery actions"
      contains: "Restore from backup"
    - path: "src/main/security/session-lock.ts"
      provides: "Launch, idle, manual, and OS-session lock enforcement"
      contains: "15 * 60 * 1000"
    - path: "src/main/security/pin-service.ts"
      provides: "PIN verification, backoff, and recovery rotation logic"
      contains: "unlockWithPin"
  key_links:
    - from: "src/main/security/session-lock.ts"
      to: "src/renderer/features/lock-screen/LockScreen.tsx"
      via: "Lock-state IPC updates"
      pattern: "lockReason|lastUnlockedAccount"
    - from: "src/renderer/features/lock-screen/RecoveryResetFlow.tsx"
      to: "src/main/security/pin-service.ts"
      via: "Recovery reset and key rotation command"
      pattern: "beginRecoveryReset|rotateRecoveryKey"
    - from: "src/renderer/features/app-shell/AppShell.tsx"
      to: "src/main/security/session-lock.ts"
      via: "Manual Lock now action"
      pattern: "Lock now"
---

<objective>
Implement the lock screen, PIN enforcement, idle and OS-session relock, manual lock action, and recovery-key reset flow.

Purpose: Complete the trusted-device security posture for phase 1.
Output: Enforced launch and idle PIN lock, branded lock screen with account context, exponential backoff, manual lock action, and recovery-key-driven PIN reset with key rotation.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-product-shell-and-security/01-CONTEXT.md
@.planning/phases/01-product-shell-and-security/01-UI-SPEC.md
@.planning/phases/01-product-shell-and-security/01-01-PLAN.md
@.planning/phases/01-product-shell-and-security/01-02-PLAN.md

<interfaces>
From src/shared/contracts/security.ts:
```typescript
export interface SecurityState {
  pinHash: string;
  failedAttempts: number;
  cooldownUntil?: string;
  recoveryCodeCiphertext: string;
  recoveryWordsCiphertext: string;
  lastUnlockedAccountLabel?: string;
}
```

From src/shared/contracts/app-state.ts:
```typescript
export interface AppShellState {
  currentView: "onboarding" | "locked" | "dashboard";
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce launch, idle, and manual lock behavior with exponential backoff</name>
  <files>src/main/security/pin-service.ts, src/main/security/session-lock.ts, src/main/ipc/security.ts, src/renderer/features/lock-screen/LockScreen.tsx, src/renderer/features/app-shell/AppShell.tsx, tests/unit/pin-service.test.ts, tests/e2e/lock-screen.spec.ts</files>
  <read_first>.planning/phases/01-product-shell-and-security/01-CONTEXT.md, .planning/phases/01-product-shell-and-security/01-UI-SPEC.md, src/shared/contracts/security.ts, src/shared/contracts/app-state.ts, src/renderer/features/app-shell/AppShell.tsx</read_first>
  <action>Implement PIN hashing and verification using `@node-rs/argon2`. Require PIN unlock on every launch after onboarding and after 15 minutes of inactivity per SECU-01. In `session-lock.ts`, lock immediately on Windows session lock or machine sleep per D-07, but do not auto-lock on minimize. Add the header action `Lock now` per D-08. In `LockScreen.tsx`, use the three-zone layout from the UI contract: brand header, account context card, and unlock panel. Show household/app name, last unlocked account label, and tertiary text actions `Restore from backup` and `Use recovery key`. Implement exponential backoff per D-06 with inline cooldown messaging such as `Try again in 30s` and no remaining-attempt counter.</action>
  <acceptance_criteria>`session-lock.ts` contains the idle timeout value for 15 minutes and logic for Windows session lock or sleep events; `LockScreen.tsx` contains the exact strings `Restore from backup`, `Use recovery key`, and `Try again in`; `AppShell.tsx` contains `Lock now`; `tests/unit/pin-service.test.ts` covers failed-attempt backoff escalation.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- pin-service && npm run test:e2e -- --grep "lock screen"</automated>
  </verify>
  <done>The app requires a PIN at the right times, offers a manual lock entry point, and slows repeated failures with visible cooldown messaging instead of permanent lockout.</done>
</task>

<task type="auto">
  <name>Task 2: Implement recovery-key reset with rotation and security-event logging</name>
  <files>src/renderer/features/lock-screen/RecoveryResetFlow.tsx, src/main/security/pin-service.ts, src/main/ipc/security.ts, src/shared/contracts/security.ts, tests/unit/pin-service.test.ts, tests/e2e/lock-screen.spec.ts</files>
  <read_first>.planning/phases/01-product-shell-and-security/01-CONTEXT.md, .planning/phases/01-product-shell-and-security/01-UI-SPEC.md, src/main/security/pin-service.ts, src/shared/contracts/security.ts, src/renderer/features/lock-screen/LockScreen.tsx</read_first>
  <action>Build the dedicated recovery reset flow described in the UI contract. Using the previously confirmed recovery key must verify the supplied code or words, force a new PIN setup, rotate to a newly generated recovery key per D-11, and append a prominent `security.recovery_key_rotated` event into the `security_events` table per D-12. Reuse the onboarding visual language for the recovery reset task but keep it distinct from settings. Show the destructive confirmation copy from the UI spec before the reset executes, then return the owner to the lock flow with the new PIN and recovery-key artifacts persisted.</action>
  <acceptance_criteria>`RecoveryResetFlow.tsx` contains the destructive confirmation copy beginning `Reset PIN with recovery key:`; `pin-service.ts` contains the exact event name `security.recovery_key_rotated`; `security.ts` exposes a `beginRecoveryReset` or equivalent reset command; tests cover successful rotation and failed recovery-key attempts.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- pin-service && npm run test:e2e -- --grep "recovery reset"</automated>
  </verify>
  <done>Recovery-key use can restore access without bypassing security, always rotates the recovery secret, and leaves an explicit audit/security trail.</done>
</task>

</tasks>

<verification>
Run the PIN-service unit tests and lock-screen e2e checks. Confirm launch lock, idle lock, manual lock, cooldown messaging, and recovery-key reset all work in one end-to-end flow.
</verification>

<success_criteria>
Phase 1 security behavior is enforceable and auditable: launch and idle relock work, unlock UI is branded and informative, and recovery reset replaces both the PIN and recovery key.
</success_criteria>

<output>
After completion, create `.planning/phases/01-product-shell-and-security/01-03-SUMMARY.md`
</output>
