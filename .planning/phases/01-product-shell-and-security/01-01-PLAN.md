---
phase: 01-product-shell-and-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - electron.vite.config.ts
  - vitest.config.ts
  - playwright.config.ts
  - src/main/main.ts
  - src/main/persistence/db.ts
  - src/main/persistence/schema.ts
  - src/main/ipc/app-state.ts
  - src/preload/index.ts
  - src/renderer/main.tsx
  - src/renderer/App.tsx
  - src/renderer/styles/tokens.css
  - src/renderer/features/app-shell/AppShell.tsx
  - src/shared/contracts/account.ts
  - src/shared/contracts/app-state.ts
  - src/shared/contracts/security.ts
  - tests/e2e/app-shell.spec.ts
  - tests/unit/app-state-contracts.test.ts
autonomous: true
requirements:
  - ONBD-01
  - SECU-01
  - SECU-02
  - ACCT-01
must_haves:
  truths:
    - "The desktop app starts inside a branded shell and can route between onboarding, locked, and dashboard surfaces."
    - "Phase 1 state is persisted locally so onboarding, lock state, and the single ICICI account profile survive app restarts."
    - "Onboarding, security, and account flows share typed contracts instead of ad hoc renderer-only state."
  artifacts:
    - path: "src/renderer/App.tsx"
      provides: "Top-level shell routing for onboarding, lock, and dashboard states"
      contains: "currentView"
    - path: "src/main/persistence/schema.ts"
      provides: "SQLite schema for onboarding progress, security state, and account profile data"
      contains: "account_profiles"
    - path: "src/shared/contracts/app-state.ts"
      provides: "Shared app-state contracts consumed by renderer and main process"
      contains: "AppShellState"
  key_links:
    - from: "src/renderer/App.tsx"
      to: "src/main/ipc/app-state.ts"
      via: "window API bridge for persisted shell state"
      pattern: "loadAppState|saveOnboardingProgress|lockNow"
    - from: "src/main/ipc/app-state.ts"
      to: "src/main/persistence/db.ts"
      via: "SQLite-backed repository calls"
      pattern: "better-sqlite3|drizzle"
    - from: "src/preload/index.ts"
      to: "src/shared/contracts/*.ts"
      via: "Typed preload bridge"
      pattern: "contextBridge"
---

<objective>
Create the Phase 1 execution foundation: a Windows desktop shell, local persistence boundary, and typed contracts for onboarding, account setup, and security.

Purpose: Prevent later plans from inventing app structure or storage ad hoc.
Output: A runnable Electron + React + TypeScript skeleton with branded shell routing, SQLite persistence bootstrap, typed IPC contracts, and automated test harnesses.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-product-shell-and-security/01-CONTEXT.md
@.planning/phases/01-product-shell-and-security/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the desktop shell and test harness</name>
  <files>package.json, tsconfig.json, electron.vite.config.ts, vitest.config.ts, playwright.config.ts, src/main/main.ts, src/preload/index.ts, src/renderer/main.tsx, src/renderer/App.tsx, src/renderer/styles/tokens.css, src/renderer/features/app-shell/AppShell.tsx, tests/e2e/app-shell.spec.ts</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-product-shell-and-security/01-CONTEXT.md, .planning/phases/01-product-shell-and-security/01-UI-SPEC.md</read_first>
  <action>Initialize the repository as an Electron + Vite + React + TypeScript desktop app. Use `electron`, `vite`, `react`, `react-dom`, `typescript`, `@vitejs/plugin-react`, `vitest`, `@testing-library/react`, `playwright`, `lucide-react`, and `@fontsource/manrope`. Build a two-layer shell from frame one: application chrome plus centered task surface. In `src/renderer/styles/tokens.css`, declare CSS variables matching the UI contract including `#F5F1E8`, `#E2D7C5`, `#0F766E`, `#B42318`, spacing tokens `--space-xs` through `--space-3xl`, and Manrope typography roles. In `src/renderer/App.tsx`, route on a `currentView` state with explicit values `onboarding`, `locked`, and `dashboard`. Add a shell header slot that can later surface `Lock now` per D-08, but do not implement lock behavior yet. Create a Playwright smoke test that asserts the shell boots and that the onboarding frame is the first rendered state.</action>
  <acceptance_criteria>`package.json` contains Electron, Vite, React, Vitest, and Playwright scripts; `src/renderer/styles/tokens.css` contains the exact hex values from the UI contract; `src/renderer/App.tsx` contains the strings `onboarding`, `locked`, and `dashboard`; `tests/e2e/app-shell.spec.ts` asserts onboarding renders first launch.</acceptance_criteria>
  <verify>
    <automated>npm install && npm run test:unit && npm run test:e2e -- --grep "app shell"</automated>
  </verify>
  <done>The repository can install dependencies, start the desktop shell, and run both unit and e2e smoke checks against the branded shell scaffold.</done>
</task>

<task type="auto">
  <name>Task 2: Define persistence and IPC contracts for onboarding, security, and the single account profile</name>
  <files>src/main/persistence/db.ts, src/main/persistence/schema.ts, src/main/ipc/app-state.ts, src/shared/contracts/account.ts, src/shared/contracts/app-state.ts, src/shared/contracts/security.ts, tests/unit/app-state-contracts.test.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-product-shell-and-security/01-CONTEXT.md, src/renderer/App.tsx, package.json</read_first>
  <action>Create a SQLite-backed persistence layer using `better-sqlite3` and `drizzle-orm`. In `src/main/persistence/schema.ts`, define tables for `app_settings`, `onboarding_progress`, `security_state`, `security_events`, and `account_profiles`. Enforce the release-1 single-account posture per D-13 and ACCT-01 by storing one `account_profiles` row with `bank_name` fixed to `ICICI`. In `src/shared/contracts/app-state.ts`, export `AppShellState`, `OnboardingStep`, and the preload API surface used by later plans: `loadAppState`, `saveOnboardingProgress`, `completeOnboarding`, `lockNow`, `unlockWithPin`, `beginRecoveryReset`, and `saveAccountProfile`. In `src/shared/contracts/security.ts`, define the persisted backoff, recovery, and lock metadata shapes. In `src/main/ipc/app-state.ts`, wire typed handlers between the preload bridge and the SQLite repository without implementing final business rules yet. Add unit tests that assert the contracts include the six onboarding steps from D-02 and that account metadata allows `openingBalance` to be optional per D-14.</action>
  <acceptance_criteria>`src/main/persistence/schema.ts` contains the table names `onboarding_progress`, `security_state`, `security_events`, and `account_profiles`; `src/shared/contracts/app-state.ts` exports `AppShellState` and `OnboardingStep`; `src/shared/contracts/account.ts` contains the exact string `ICICI`; `tests/unit/app-state-contracts.test.ts` checks six onboarding steps and optional opening balance metadata.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- app-state-contracts</automated>
  </verify>
  <done>Later plans can build against stable shell, onboarding, security, and account contracts without reworking persistence or IPC boundaries.</done>
</task>

</tasks>

<verification>
Run the unit suite and the shell smoke test. Confirm the app boots into the onboarding state and the SQLite/IPC contracts exist before parallel work begins.
</verification>

<success_criteria>
The repository has a runnable desktop scaffold, local persistence boundaries, and shared contracts that make the onboarding and security plans implementation-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/01-product-shell-and-security/01-01-SUMMARY.md`
</output>
