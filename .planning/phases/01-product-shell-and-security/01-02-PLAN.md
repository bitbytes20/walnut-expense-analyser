---
phase: 01-product-shell-and-security
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/renderer/features/onboarding/OnboardingFlow.tsx
  - src/renderer/features/onboarding/steps/WelcomeStep.tsx
  - src/renderer/features/onboarding/steps/HouseholdProfileStep.tsx
  - src/renderer/features/onboarding/steps/PinSetupStep.tsx
  - src/renderer/features/onboarding/steps/RecoveryKeyStep.tsx
  - src/renderer/features/onboarding/steps/AccountProfileStep.tsx
  - src/renderer/features/onboarding/steps/FinishStep.tsx
  - src/renderer/features/dashboard/EmptyDashboard.tsx
  - src/renderer/features/onboarding/onboardingMachine.ts
  - src/shared/contracts/account.ts
  - tests/unit/onboarding-machine.test.ts
  - tests/e2e/onboarding-flow.spec.ts
autonomous: true
requirements:
  - ONBD-01
  - SECU-02
  - ACCT-01
must_haves:
  truths:
    - "The owner can complete a linear onboarding wizard with the exact six-step order locked in D-02."
    - "Closing and reopening the app resumes onboarding at the last incomplete step per D-03."
    - "The owner must confirm recovery-key safekeeping before finishing onboarding, and the account step enforces the single ICICI profile posture."
  artifacts:
    - path: "src/renderer/features/onboarding/OnboardingFlow.tsx"
      provides: "Linear onboarding wizard with Back and Next controls"
      contains: "Step 1 of 6"
    - path: "src/renderer/features/onboarding/steps/RecoveryKeyStep.tsx"
      provides: "Recovery code/words presentation with copy or download gating"
      contains: "saved on this device"
    - path: "src/renderer/features/dashboard/EmptyDashboard.tsx"
      provides: "Post-onboarding empty dashboard landing state"
      contains: "Ready for your first import"
  key_links:
    - from: "src/renderer/features/onboarding/onboardingMachine.ts"
      to: "src/shared/contracts/app-state.ts"
      via: "Persisted step transitions"
      pattern: "saveOnboardingProgress|completeOnboarding"
    - from: "src/renderer/features/onboarding/steps/AccountProfileStep.tsx"
      to: "src/shared/contracts/account.ts"
      via: "Single-account form model"
      pattern: "ICICI|openingBalance"
    - from: "src/renderer/features/onboarding/steps/FinishStep.tsx"
      to: "src/renderer/features/dashboard/EmptyDashboard.tsx"
      via: "Completion route handoff"
      pattern: "Ready for your first import"
---

<objective>
Implement the complete first-run onboarding experience for the owner, including household profile, PIN creation input, recovery-key confirmation, optional account creation, and the empty dashboard handoff.

Purpose: Satisfy the guided setup and single-account requirements without leaving any onboarding step ambiguous.
Output: A persisted six-step onboarding wizard, recovery-key confirmation gates, single-account ICICI account setup or skip flow, and the post-onboarding empty dashboard state.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-product-shell-and-security/01-CONTEXT.md
@.planning/phases/01-product-shell-and-security/01-UI-SPEC.md
@.planning/phases/01-product-shell-and-security/01-01-PLAN.md

<interfaces>
From src/shared/contracts/app-state.ts:
```typescript
export type OnboardingStep =
  | "welcome"
  | "household-profile"
  | "pin-setup"
  | "recovery-key"
  | "account-profile"
  | "finish";

export interface AppShellState {
  currentView: "onboarding" | "locked" | "dashboard";
  onboarding: { currentStep: OnboardingStep; completedSteps: OnboardingStep[] };
}
```

From src/shared/contracts/account.ts:
```typescript
export interface AccountProfileDraft {
  bankName: "ICICI";
  displayName: string;
  accountHolderName: string;
  maskedAccountNumber?: string;
  nickname?: string;
  baseCurrency: string;
  openingBalance?: number;
  openingBalanceDate?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the persisted six-step onboarding wizard</name>
  <files>src/renderer/features/onboarding/OnboardingFlow.tsx, src/renderer/features/onboarding/steps/WelcomeStep.tsx, src/renderer/features/onboarding/steps/HouseholdProfileStep.tsx, src/renderer/features/onboarding/steps/PinSetupStep.tsx, src/renderer/features/onboarding/steps/FinishStep.tsx, src/renderer/features/onboarding/onboardingMachine.ts, tests/unit/onboarding-machine.test.ts</files>
  <read_first>.planning/phases/01-product-shell-and-security/01-CONTEXT.md, .planning/phases/01-product-shell-and-security/01-UI-SPEC.md, .planning/phases/01-product-shell-and-security/01-01-PLAN.md, src/shared/contracts/app-state.ts, src/renderer/App.tsx</read_first>
  <action>Build the renderer onboarding state machine as a linear wizard per D-01 and D-02 with the exact order `welcome`, `household-profile`, `pin-setup`, `recovery-key`, `account-profile`, `finish`. Display progress as plain language `Step X of 6`, keep primary action bottom-right and secondary action bottom-left, and show the helper text `Progress saved on this device` after successful step persistence. The wizard must resume from the last incomplete step after app restart per D-03. Keep PIN creation on its own screen, show `minimum 6 digits, numeric only`, and validate on blur and submit. Do not add a numbered stepper, clickable milestones, or combine multiple primary tasks on one screen.</action>
  <acceptance_criteria>`src/renderer/features/onboarding/onboardingMachine.ts` contains all six step IDs in the D-02 order; `OnboardingFlow.tsx` renders the exact pattern `Step ${` or `Step 1 of 6`; `PinSetupStep.tsx` contains `minimum 6 digits, numeric only`; `tests/unit/onboarding-machine.test.ts` covers resume-at-last-incomplete-step behavior.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- onboarding-machine</automated>
  </verify>
  <done>The wizard can advance and go back linearly, persist progress step by step, and restore the saved incomplete step instead of restarting.</done>
</task>

<task type="auto">
  <name>Task 2: Add recovery-key confirmation, account setup or skip, and empty-dashboard handoff</name>
  <files>src/renderer/features/onboarding/steps/RecoveryKeyStep.tsx, src/renderer/features/onboarding/steps/AccountProfileStep.tsx, src/renderer/features/dashboard/EmptyDashboard.tsx, src/shared/contracts/account.ts, tests/e2e/onboarding-flow.spec.ts</files>
  <read_first>.planning/phases/01-product-shell-and-security/01-CONTEXT.md, .planning/phases/01-product-shell-and-security/01-UI-SPEC.md, src/shared/contracts/account.ts, src/shared/contracts/security.ts, src/renderer/features/onboarding/OnboardingFlow.tsx</read_first>
  <action>Implement the recovery-key step per D-09 and D-10 with synchronized segmented code and recovery-words presentations, adjacent `Copy` and `Download` actions, and an explicit confirmation control that stays disabled until either copy or download completes. Implement the account setup step per D-13 through D-16: fixed read-only bank field `ICICI`, required `account display name`, `account holder name`, and `base currency`, optional masked account number or nickname, and optional opening balance metadata. Add a visible `Skip for now` action that sets a lazy-account-creation flag for first import. Finishing onboarding must route to a dashboard empty state with the exact heading `Ready for your first import`, the empty-state body from the UI spec, and an import-oriented primary CTA.</action>
  <acceptance_criteria>`RecoveryKeyStep.tsx` contains copy and download actions plus disabled-until-confirmed progression logic; `AccountProfileStep.tsx` contains the exact read-only bank label `ICICI` and the string `Skip for now`; `EmptyDashboard.tsx` contains the exact heading `Ready for your first import`; `tests/e2e/onboarding-flow.spec.ts` covers recovery-key gating and both account-create and skip paths.</acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- --grep "onboarding flow"</automated>
  </verify>
  <done>Onboarding cannot complete without recovery-key confirmation, supports either account creation or explicit skip, and lands in the empty dashboard state described by the approved UI contract.</done>
</task>

</tasks>

<verification>
Run the onboarding unit and e2e suites. Confirm first-run completion reaches the empty dashboard and that app restart resumes the last incomplete step.
</verification>

<success_criteria>
The owner can finish a guided, resumable onboarding flow that sets up trust, confirms recovery posture, and establishes the release-1 account path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-product-shell-and-security/01-02-SUMMARY.md`
</output>
