---
phase: 04-transaction-ledger-and-search
plan: 04
type: execute
wave: 4
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
  - 04-03-PLAN.md
files_modified:
  - tests/unit/transactions/normalization.test.ts
  - tests/unit/transactions/query-transactions.test.ts
  - tests/unit/transactions/update-transaction.test.ts
  - tests/unit/transaction-ledger.test.tsx
  - tests/e2e/transaction-ledger.spec.ts
  - .planning/phases/04-transaction-ledger-and-search/04-VERIFICATION.md
autonomous: true
requirements:
  - TRAN-01
  - TRAN-02
  - TRAN-04
  - TRAN-05
must_haves:
  truths:
    - "Phase 4 is not complete until normalization, search/filtering, and drawer editing are covered by both unit and end-to-end tests."
    - "Accepted transactions from review-pending batches remain visible in the ledger."
    - "Keyboard and responsiveness expectations are verified, not assumed."
---

<objective>
Close Phase 4 with complete regression coverage, responsiveness checks, and a formal verification handoff.
</objective>

<context>
@.planning/phases/04-transaction-ledger-and-search/04-VALIDATION.md
@.planning/phases/04-transaction-ledger-and-search/04-UI-SPEC.md
@tests/unit/transaction-ledger.test.tsx
@tests/unit/transactions/normalization.test.ts
@tests/unit/transactions/query-transactions.test.ts
@tests/unit/transactions/update-transaction.test.ts
</context>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Fill the Phase 4 unit and end-to-end coverage gaps</name>
  <files>tests/unit/transactions/normalization.test.ts, tests/unit/transactions/query-transactions.test.ts, tests/unit/transactions/update-transaction.test.ts, tests/unit/transaction-ledger.test.tsx, tests/e2e/transaction-ledger.spec.ts</files>
  <behavior>
    - verify normalization coverage for baseline and confident special types
    - verify search and all required filters
    - verify drawer editing and tag persistence
    - verify the end-to-end browse, filter, edit, and refresh loop
  </behavior>
  <action>Fill the Wave 0 gaps declared in the validation contract. Add or expand the unit suites for normalization, query behavior, and transaction updates. Add component coverage for the transactions screen and edit drawer. Add a Playwright spec that opens the Transactions workspace, searches, filters, edits a row, and verifies the updated values persist in the visible ledger.</action>
  <acceptance_criteria>All planned unit and end-to-end Phase 4 tests exist and pass; the e2e flow covers browsing, search, filter, edit, and persistence confirmation.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/transactions/*.test.ts tests/unit/transaction-ledger.test.tsx && cmd /c npx playwright test tests/e2e/transaction-ledger.spec.ts</automated>
  </verify>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Complete formal verification and execution handoff</name>
  <files>.planning/phases/04-transaction-ledger-and-search/04-VERIFICATION.md</files>
  <behavior>
    - record the executed commands and outcomes
    - map validation back to TRAN-01, TRAN-02, TRAN-04, and TRAN-05
    - note any remaining residual risks or polish gaps clearly
  </behavior>
  <action>Write the Phase 4 verification report after tests pass. Include the executed commands, requirement mapping, keyboard/responsiveness notes, and any residual follow-up items that should be tracked later instead of quietly slipping scope.</action>
  <acceptance_criteria>`04-VERIFICATION.md` exists, references the final commands, and closes the loop on all Phase 4 requirements.</acceptance_criteria>
  <verify>
    <automated>cmd /c npm run test:unit && cmd /c npx playwright test tests/e2e/transaction-ledger.spec.ts</automated>
  </verify>
</task>
</tasks>

<output>
After completion, create `.planning/phases/04-transaction-ledger-and-search/04-04-SUMMARY.md`
</output>
