---
phase: 04-transaction-ledger-and-search
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - src/main/persistence/db.ts
  - src/main/ipc/transactions.ts
  - src/shared/contracts/transactions.ts
  - src/preload/index.ts
  - src/renderer/features/transactions/TransactionDetailDrawer.tsx
  - src/renderer/features/transactions/TransactionsScreen.tsx
  - tests/unit/transactions/update-transaction.test.ts
  - tests/unit/transaction-ledger.test.tsx
autonomous: true
requirements:
  - TRAN-02
  - TRAN-04
must_haves:
  truths:
    - "Ledger edits happen from a right-side detail drawer and save immediately."
    - "Editable fields match the locked Phase 4 field set exactly."
    - "Manual type changes can surface rule-suggestion metadata without turning this phase into full rule management."
---

<objective>
Add the transaction edit drawer and immediate-save mutation flow so users can correct imported transactions directly from the ledger.
</objective>

<context>
@.planning/phases/04-transaction-ledger-and-search/04-CONTEXT.md
@.planning/phases/04-transaction-ledger-and-search/04-RESEARCH.md
@.planning/phases/04-transaction-ledger-and-search/04-UI-SPEC.md
@src/shared/contracts/transactions.ts
@src/main/persistence/db.ts
@src/renderer/features/transactions/TransactionsScreen.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement authoritative transaction update mutations and drawer contracts</name>
  <files>src/shared/contracts/transactions.ts, src/main/persistence/db.ts, src/main/ipc/transactions.ts, src/preload/index.ts, tests/unit/transactions/update-transaction.test.ts</files>
  <behavior>
    - updates persist amount, date, description, type, category, tags, reference, and review-state override
    - the update path can return rule-suggestion metadata when a user manually changes type
    - invalid or partial updates are rejected cleanly through the shared contract
  </behavior>
  <action>Add the `updateTransaction` contract and mutation path in shared contracts, persistence, IPC, and preload. Keep the mutation authoritative and repository-backed. Capture enough metadata to support later rule suggestion flows when type changes occur, but do not implement full reusable rule authoring in this wave.</action>
  <acceptance_criteria>`src/main/persistence/db.ts` contains a stable `updateTransaction` mutation; shared contracts validate the approved editable fields; mutation tests cover every Phase 4 editable field and the rule-suggestion hook.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/transactions/update-transaction.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the right-side transaction detail drawer and immediate-save workflow</name>
  <files>src/renderer/features/transactions/TransactionDetailDrawer.tsx, src/renderer/features/transactions/TransactionsScreen.tsx, tests/unit/transaction-ledger.test.tsx, tests/unit/transactions/update-transaction.test.ts</files>
  <behavior>
    - selecting a row opens the right-side drawer with snapshot context
    - saving drawer edits updates the ledger after authoritative refetch
    - tags, type, category, reference, and review-state override are editable without leaving the transactions screen
  </behavior>
  <action>Implement the two-section drawer UI with a transaction snapshot and a lower editable form/action section. Keep save behavior immediate and local-feeling, then refetch authoritative row/list state after mutation. Add subtle rule-suggestion messaging only when manual type changes occur.</action>
  <acceptance_criteria>The drawer opens from the ledger, shows snapshot context and editable fields in the approved split, saves immediately, and refreshes the row/list state after mutation; renderer tests cover open, edit, save, and refresh behavior.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/transaction-ledger.test.tsx tests/unit/transactions/update-transaction.test.ts</automated>
  </verify>
</task>
</tasks>

<output>
After completion, create `.planning/phases/04-transaction-ledger-and-search/04-03-SUMMARY.md`
</output>
