---
phase: 04-transaction-ledger-and-search
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - src/renderer/App.tsx
  - src/renderer/features/app-shell/AppShell.tsx
  - src/renderer/features/transactions/TransactionsScreen.tsx
  - src/renderer/features/transactions/TransactionLedgerTable.tsx
  - src/renderer/features/transactions/TransactionFilterDrawer.tsx
  - src/renderer/features/transactions/TransactionEmptyState.tsx
  - tests/unit/transaction-ledger.test.tsx
autonomous: true
requirements:
  - TRAN-05
must_haves:
  truths:
    - "Transactions becomes a first-class left-rail destination in the existing Walnut shell."
    - "The ledger is dense, newest-first, and centered on quick search plus an advanced filter drawer."
    - "The default row surface stays minimal and scannable."
---

<objective>
Build the Phase 4 transactions workspace in the renderer: left-rail routing, dense ledger presentation, quick search, and the advanced filter drawer.
</objective>

<context>
@.planning/phases/04-transaction-ledger-and-search/04-CONTEXT.md
@.planning/phases/04-transaction-ledger-and-search/04-RESEARCH.md
@.planning/phases/04-transaction-ledger-and-search/04-UI-SPEC.md
@src/renderer/App.tsx
@src/renderer/features/app-shell/AppShell.tsx
@src/shared/contracts/transactions.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add Transactions to the app shell and build the dense ledger screen</name>
  <files>src/renderer/App.tsx, src/renderer/features/app-shell/AppShell.tsx, src/renderer/features/transactions/TransactionsScreen.tsx, src/renderer/features/transactions/TransactionLedgerTable.tsx, src/renderer/features/transactions/TransactionEmptyState.tsx, tests/unit/transaction-ledger.test.tsx</files>
  <behavior>
    - the left rail exposes a dedicated Transactions destination
    - the ledger renders newest-first rows with the default Phase 4 columns
    - empty and no-result states use the locked Phase 4 copy
  </behavior>
  <action>Create the new transactions feature folder and build a dense ledger screen inside the current Walnut shell. Add `Transactions` to the left rail. Keep the row surface minimal and aligned with the UI spec: `date`, `description`, `amount`, `type`, and `tags` only by default.</action>
  <acceptance_criteria>The shell routes to a dedicated Transactions screen; the ledger defaults to newest-first rows and the required columns; empty/no-result states use the locked copy; renderer tests fail before implementation and then pass.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/transaction-ledger.test.tsx</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire quick search and the advanced filter drawer</name>
  <files>src/renderer/features/transactions/TransactionsScreen.tsx, src/renderer/features/transactions/TransactionFilterDrawer.tsx, src/renderer/features/transactions/TransactionLedgerTable.tsx, tests/unit/transaction-ledger.test.tsx</files>
  <behavior>
    - quick search narrows rows by description, reference, or tags
    - the filter drawer applies date range, type, amount range, category, tags, and review state
    - clearing filters restores the broader list
  </behavior>
  <action>Wire the search bar and advanced filter drawer to the transaction query surface created in Wave 1. Keep quick search visible in the main header and structured filters in a dedicated drawer so the toolbar stays clean. Use deferred/concurrent React update patterns where appropriate so typing stays responsive.</action>
  <acceptance_criteria>The transaction screen can search by text and apply the full set of Phase 4 filters from the drawer; filter state and row results stay in sync; renderer tests cover search, apply, and clear flows.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/transaction-ledger.test.tsx tests/unit/transactions/query-transactions.test.ts</automated>
  </verify>
</task>
</tasks>

<output>
After completion, create `.planning/phases/04-transaction-ledger-and-search/04-02-SUMMARY.md`
</output>
