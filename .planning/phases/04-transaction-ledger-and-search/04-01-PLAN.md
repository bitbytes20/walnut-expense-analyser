---
phase: 04-transaction-ledger-and-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/contracts/app-state.ts
  - src/shared/contracts/import.ts
  - src/shared/contracts/transactions.ts
  - src/preload/index.ts
  - src/main/persistence/schema.ts
  - src/main/persistence/db.ts
  - src/main/ipc/transactions.ts
  - src/main/import/import-coordinator.ts
  - tests/unit/transactions/normalization.test.ts
  - tests/unit/transactions/query-transactions.test.ts
autonomous: true
requirements:
  - TRAN-01
  - TRAN-05
must_haves:
  truths:
    - "Imported transactions remain the source of truth; Phase 4 does not create a second ledger store."
    - "Baseline expense and income typing is automatic, while confident special types are assigned conservatively and remain user-overrideable."
    - "Ledger queries support the required search and filter facets over authoritative local data."
---

<objective>
Create the backend foundation for the transaction workspace: shared contracts, normalized transaction types, and authoritative repository query APIs for ledger search and filtering.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transaction-ledger-and-search/04-CONTEXT.md
@.planning/phases/04-transaction-ledger-and-search/04-RESEARCH.md
@.planning/phases/04-transaction-ledger-and-search/04-UI-SPEC.md
@.planning/phases/04-transaction-ledger-and-search/04-VALIDATION.md
@src/main/persistence/db.ts
@src/shared/contracts/import.ts
@src/shared/contracts/app-state.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define transaction-ledger contracts and conservative normalization rules</name>
  <files>src/shared/contracts/app-state.ts, src/shared/contracts/import.ts, src/shared/contracts/transactions.ts, src/preload/index.ts, tests/unit/transactions/normalization.test.ts</files>
  <behavior>
    - debit and credit rows normalize to baseline `expense` and `income`
    - confident transfer, refund, ATM withdrawal, and credit-card payment descriptions normalize to special types without aggressive overreach
    - shared list/detail query contracts model search, tags, date range, amount range, type, category, and review-state filtering
  </behavior>
  <action>Create a dedicated `src/shared/contracts/transactions.ts` module for ledger rows, list queries, detail responses, update inputs, and rule-suggestion metadata. Add the transaction API methods to `WalnutApi` and mirror them through preload. Write normalization tests first so conservative typing behavior is locked before repository code lands.</action>
  <acceptance_criteria>`src/shared/contracts/transactions.ts` contains typed schemas for list, detail, and update flows; the normalized type union includes all required Phase 4 types; preload and `WalnutApi` expose list/detail transaction methods; normalization tests fail before implementation and explicitly cover conservative special-type detection.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/transactions/normalization.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend persistence and IPC with transaction list/detail query support</name>
  <files>src/main/persistence/schema.ts, src/main/persistence/db.ts, src/main/ipc/transactions.ts, src/preload/index.ts, src/main/import/import-coordinator.ts, tests/unit/transactions/query-transactions.test.ts, tests/unit/transactions/normalization.test.ts</files>
  <behavior>
    - transaction queries respect text search over description, reference, and tags
    - structured filters for date range, type, amount range, category, and review state return the correct rows
    - newest-first ordering is the default query behavior
  </behavior>
  <action>Extend the imported transaction schema and repository methods to persist normalized type and the additional fields needed for drawer editing and ledger filtering. Add repository queries for list and detail access, keeping the imported-transaction table as the source of truth. Register dedicated transaction list/detail IPC handlers and preload methods.</action>
  <acceptance_criteria>`src/main/persistence/db.ts` exposes `listTransactions` and `getTransactionDetail`; IPC and preload expose matching handlers; repository queries support the Phase 4 search/filter facets and default ordering; normalization and query tests pass.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/transactions/normalization.test.ts tests/unit/transactions/query-transactions.test.ts</automated>
  </verify>
</task>
</tasks>

<output>
After completion, create `.planning/phases/04-transaction-ledger-and-search/04-01-SUMMARY.md`
</output>
