---
phase: 02-statement-import-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/main/persistence/schema.ts
  - src/main/persistence/db.ts
  - src/main/import/duplicate-service.ts
  - src/main/import/import-service.ts
  - src/main/ipc/import.ts
  - src/main/main.ts
  - tests/unit/import/persistence.test.ts
  - tests/unit/import/duplicates.test.ts
autonomous: true
requirements:
  - IMPT-03
  - IMPT-04
  - IMPT-01
must_haves:
  truths:
    - "Importing a staged batch persists only normalized rows plus import metadata, not statement files."
    - "A statement already imported is blocked even when renamed, and the block explains which earlier batch caused it."
    - "The first successful import can create the skipped ICICI account profile inside the same local persistence path."
  artifacts:
    - path: "src/main/persistence/schema.ts"
      provides: "SQLite tables for import batches, import files, and imported transactions"
      contains: "import_batches"
    - path: "src/main/import/import-service.ts"
      provides: "Main-process stage/commit orchestration with duplicate checks and lazy account creation"
      contains: "commitImportBatch"
    - path: "tests/unit/import/duplicates.test.ts"
      provides: "Regression coverage for fingerprint and transaction-signature duplicate blocking"
      contains: "sha256"
  key_links:
    - from: "src/main/ipc/import.ts"
      to: "src/main/import/import-service.ts"
      via: "Main-process IPC handlers for import actions"
      pattern: "import:stage-files|import:commit-batch"
    - from: "src/main/import/import-service.ts"
      to: "src/main/persistence/db.ts"
      via: "SQLite transaction that writes batches, files, and normalized rows"
      pattern: "transaction|saveAccountProfile"
    - from: "src/main/import/duplicate-service.ts"
      to: "src/main/persistence/schema.ts"
      via: "Stored file fingerprints and transaction signatures"
      pattern: "file_fingerprint_sha256|transaction_signature"
---

<objective>
Implement the repository-backed import commit flow that persists records only, blocks clear duplicates, and auto-creates the ICICI account profile on first successful import when onboarding skipped it.

Purpose: Enforce Phase 2 trust rules in durable local storage rather than leaving them as UI-only behavior.
Output: Import tables, repository methods, duplicate service, IPC handlers, and unit tests for persistence and duplicate enforcement.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-statement-import-pipeline/02-CONTEXT.md
@.planning/phases/02-statement-import-pipeline/02-RESEARCH.md
@.planning/phases/02-statement-import-pipeline/02-VALIDATION.md
@.planning/phases/02-statement-import-pipeline/02-01-PLAN.md
@.planning/phases/01-product-shell-and-security/01-01-SUMMARY.md
@.planning/phases/01-product-shell-and-security/01-02-SUMMARY.md

<interfaces>
From src/shared/contracts/import.ts:
```typescript
export type ImportStageStatus =
  | "ready"
  | "needs-sheet-selection"
  | "rejected"
  | "duplicate-blocked";

export interface PriorImportBatchReference {
  batchId: string;
  importedAt: string;
  fileName: string;
  transactionCount: number;
}
```

From src/main/persistence/db.ts:
```typescript
export class WalnutRepository {
  loadAppState(): AppShellState;
  saveAccountProfile(draft: AccountProfileDraft): AppShellState;
  loadAccountProfile(): AccountProfile | undefined;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SQLite persistence for import batches, files, and transactions without storing source files</name>
  <files>src/main/persistence/schema.ts, src/main/persistence/db.ts, tests/unit/import/persistence.test.ts</files>
  <read_first>src/main/persistence/schema.ts, src/main/persistence/db.ts, tests/unit/import/persistence.test.ts, src/shared/contracts/account.ts, .planning/PROJECT.md, .planning/phases/02-statement-import-pipeline/02-CONTEXT.md, .planning/phases/02-statement-import-pipeline/02-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Successful commit writes an import batch row, import file rows, and imported transaction rows.
    - Test 2: No table stores raw statement bytes, original file paths, or copied attachments per IMPT-03.
    - Test 3: When the account profile was skipped during onboarding, the first successful import creates or updates the single ICICI account profile.
  </behavior>
  <action>In `src/main/persistence/schema.ts`, add concrete tables `import_batches`, `import_files`, and `import_transactions` with columns for `id`, `status`, `imported_at`, `source_file_name`, `source_file_identifier`, `file_fingerprint_sha256`, `sheet_name`, `duplicate_reason`, `prior_batch_id`, `transaction_signature`, `transaction_date`, `value_date`, `raw_narration`, `cleaned_description`, `debit_amount_minor`, `credit_amount_minor`, `running_balance_minor`, `direction`, `statement_reference`, `warning_codes_json`, and `batch_id`. Do not add blob or path columns for source file retention. In `src/main/persistence/db.ts`, add repository methods `createImportBatch`, `replaceStagedImportBatch`, `commitImportedTransactions`, `findPriorBatchReference`, and `loadImportBatchSummary` using one SQLite transaction. Reuse `saveAccountProfile` for lazy account creation so the first successful import can set `bankName: 'ICICI'`, `displayName`, `accountHolderName`, and `skippedDuringOnboarding: false` through the existing account profile path.</action>
  <acceptance_criteria>`src/main/persistence/schema.ts` contains the exact table names `import_batches`, `import_files`, and `import_transactions`; the schema contains the exact column names `file_fingerprint_sha256`, `transaction_signature`, and `prior_batch_id`; the schema does not contain the strings `BLOB`, `file_path`, or `source_bytes`; `tests/unit/import/persistence.test.ts` asserts lazy account creation and record-only storage.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/import/persistence.test.ts</automated>
  </verify>
  <done>The database can store durable import metadata and normalized rows while staying compliant with the no-file-retention rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement duplicate-safe import orchestration and main-process IPC handlers</name>
  <files>src/main/import/duplicate-service.ts, src/main/import/import-service.ts, src/main/ipc/import.ts, src/main/main.ts, tests/unit/import/duplicates.test.ts</files>
  <read_first>src/main/import/duplicate-service.ts, src/main/import/import-service.ts, src/main/main.ts, tests/unit/import/duplicates.test.ts, src/shared/contracts/import.ts, src/main/persistence/db.ts, .planning/phases/02-statement-import-pipeline/02-CONTEXT.md, .planning/phases/02-statement-import-pipeline/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1: A renamed file with identical bytes is blocked by `file_fingerprint_sha256`.
    - Test 2: A regenerated export with matching normalized transaction signatures is blocked even when the file fingerprint differs.
    - Test 3: Duplicate results include the reason and prior batch reference required for UI inspection.
  </behavior>
  <action>Create `src/main/import/duplicate-service.ts` using `node:crypto` SHA-256 for file fingerprints and deterministic transaction signatures built from `transactionDateRaw`, `valueDateRaw`, `direction`, `debitAmountMinor`, `creditAmountMinor`, normalized narration, `statementReference`, and `runningBalanceMinor`. Allow small formatting tolerance by computing signatures from trimmed uppercase narration and stripped punctuation, but do not add fuzzy amount matching beyond exact integer paise values. In `src/main/import/import-service.ts`, orchestrate `stageImportFiles`, `chooseImportSheet`, `removeStagedFile`, `commitImportBatch`, and `inspectEarlierBatch`. Re-check duplicates inside the same SQLite transaction before inserts. Mark duplicate files with the exact reason codes `same-file-fingerprint` or `matching-transaction-signatures`, populate `priorBatchId`, and return `PriorImportBatchReference` details. Register handlers in `src/main/ipc/import.ts` for the exact channels exposed by preload, and wire that module from `src/main/main.ts` alongside the existing app-state and security registration.</action>
  <acceptance_criteria>`src/main/import/duplicate-service.ts` contains the exact string `createHash('sha256')`; `src/main/import/import-service.ts` contains the exact method names `stageImportFiles`, `chooseImportSheet`, `removeStagedFile`, `commitImportBatch`, and `inspectEarlierBatch`; the import service contains the exact duplicate reason strings `same-file-fingerprint` and `matching-transaction-signatures`; `src/main/main.ts` imports and registers `registerImportIpc`; `tests/unit/import/duplicates.test.ts` asserts renamed-file and matching-transaction duplicate blocking.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/import/duplicates.test.ts tests/unit/import/persistence.test.ts</automated>
  </verify>
  <done>Import commit behavior is durable, duplicate-safe, and explainable through prior-batch references instead of opaque blocking.</done>
</task>

</tasks>

<verification>
Run the persistence and duplicate suites. Confirm imported data is stored without retained files, renamed re-imports are blocked, and the first successful import can create the ICICI account profile if it was skipped earlier.
</verification>

<success_criteria>
Phase 2 enforces IMPT-03 and IMPT-04 at the repository and main-process boundary, not just in the renderer.
</success_criteria>

<output>
After completion, create `.planning/phases/02-statement-import-pipeline/02-02-SUMMARY.md`
</output>
