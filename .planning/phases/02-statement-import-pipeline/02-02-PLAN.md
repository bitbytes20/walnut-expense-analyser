---
phase: 02-statement-import-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/preload/index.ts
  - src/shared/contracts/account.ts
  - src/shared/contracts/import.ts
  - src/main/persistence/schema.ts
  - src/main/import/file-picker.ts
  - src/main/import/parser.ts
  - src/main/import/duplicate-service.ts
  - src/main/import/normalizers.ts
  - src/main/import/worksheet-selector.ts
  - src/main/ipc/import.ts
  - src/main/persistence/db.ts
  - tests/unit/import/parser.test.ts
  - tests/unit/import/duplicates.test.ts
  - tests/unit/import/persistence.test.ts
autonomous: true
requirements:
  - IMPT-01
  - IMPT-02
  - IMPT-03
  - IMPT-04
must_haves:
  truths:
    - "Supported ICICI CSV, XLS, and XLSX files import through one deterministic on-device pipeline."
    - "Unsupported files fail with exact reasons and worksheet ambiguity is resolved before import commit."
    - "Clear duplicates are blocked with prior-batch context while imported records persist without retaining statement files."
  artifacts:
    - path: "src/main/import/parser.ts"
      provides: "Best-effort ICICI parser using recognizable columns, raw-field preservation, and basic debit/credit normalization"
      contains: "rawNarration"
    - path: "src/main/import/duplicate-service.ts"
      provides: "File fingerprint and transaction signature duplicate checks"
      contains: "sha256"
    - path: "tests/unit/import/duplicates.test.ts"
      provides: "Deterministic duplicate-blocking coverage"
      contains: "duplicate-blocked"
  key_links:
    - from: "src/main/ipc/import.ts"
      to: "src/main/import/parser.ts"
      via: "Stage and commit import orchestration"
      pattern: "stageImportFiles|commitImportBatch"
    - from: "src/main/import/parser.ts"
      to: "src/main/import/worksheet-selector.ts"
      via: "Plausible-sheet detection before parse commit"
      pattern: "recommended"
    - from: "src/main/import/duplicate-service.ts"
      to: "src/main/persistence/db.ts"
      via: "Prior-batch lookup and duplicate persistence"
      pattern: "findDuplicateImport"
---

<objective>
Implement the main-process import engine: file staging, workbook inspection, sheet selection, normalization, transactional record-only persistence, duplicate checks, and earlier-batch inspection plumbing.

Purpose: Deliver the actual trusted ingestion pipeline behind the renderer workspace.
Output: Main-process import services that stage files, parse supported ICICI statements, reject unsupported variants clearly, persist normalized records without retained files, and block clear duplicates with inspectable prior-batch context.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-statement-import-pipeline/02-CONTEXT.md
@.planning/phases/02-statement-import-pipeline/02-RESEARCH.md
@.planning/phases/02-statement-import-pipeline/02-UI-SPEC.md
@.planning/phases/02-statement-import-pipeline/02-VALIDATION.md
@.planning/phases/02-statement-import-pipeline/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build main-process staging, parser normalization, and record-only persistence</name>
  <files>src/preload/index.ts, src/shared/contracts/import.ts, src/shared/contracts/account.ts, src/main/persistence/schema.ts, src/main/persistence/db.ts, src/main/import/file-picker.ts, src/main/import/parser.ts, src/main/import/worksheet-selector.ts, src/main/import/normalizers.ts, src/main/ipc/import.ts, tests/unit/import/parser.test.ts, tests/unit/import/persistence.test.ts</files>
  <read_first>.planning/phases/02-statement-import-pipeline/02-CONTEXT.md, .planning/phases/02-statement-import-pipeline/02-RESEARCH.md, .planning/phases/02-statement-import-pipeline/02-UI-SPEC.md, src/main/ipc/app-state.ts, src/main/persistence/schema.ts, src/main/persistence/db.ts, src/shared/contracts/import.ts, src/shared/contracts/account.ts, tests/fixtures/import/icici-valid.csv, tests/fixtures/import/icici-valid.xlsx</read_first>
  <action>Create `src/main/import/file-picker.ts`, `parser.ts`, `worksheet-selector.ts`, and `normalizers.ts`, and finish the real import IPC bridge in `src/main/ipc/import.ts` plus `src/preload/index.ts`. In the main process, open the file picker for `csv`, `xls`, and `xlsx`, read files on-device, and return only staged metadata to the renderer. Use `xlsx` as the single loader for CSV/XLS/XLSX, normalize header labels before matching, and treat a file as supported when key ICICI columns are recognizable rather than requiring one exact template. Preserve the raw fields from D-09 exactly: transaction date, value date, raw narration, debit amount, credit amount, running balance, reference/cheque/transaction ID when present, plus source file and batch IDs. Derive only `direction: debit|credit`, keep both `rawNarration` and `cleanedDescription`, and soft-check balance continuity with warning output instead of hard failure. In Excel flows, detect plausible sheets, mark one `recommended`, and return `needs-sheet-selection` when more than one plausible worksheet exists. In `src/main/persistence/schema.ts` and `src/main/persistence/db.ts`, create the exact tables `import_batches`, `import_source_files`, and `imported_transactions`, store only import metadata and normalized transaction data, and never copy original statement bytes into SQLite or app-managed storage. Persist money values as integer minor units and support lazy creation of the single ICICI account profile on the first successful import through the existing account persistence path.</action>
  <acceptance_criteria>`src/main/import/parser.ts` contains `rawNarration`, `cleanedDescription`, and `direction`; `src/main/import/worksheet-selector.ts` contains the string `recommended`; `src/main/ipc/import.ts` registers `import:stage-files`, `import:choose-sheet`, and `import:commit-batch`; `src/main/persistence/schema.ts` contains `import_batches`, `import_source_files`, and `imported_transactions`; `tests/unit/import/parser.test.ts` asserts CSV, XLS, and XLSX parsing plus multi-sheet ambiguity; `tests/unit/import/persistence.test.ts` asserts no original file blobs or copied file contents are persisted.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/import/parser.test.ts tests/unit/import/persistence.test.ts</automated>
  </verify>
  <done>The main process can stage supported statement files, detect sheet ambiguity, normalize rows into the Phase 2 contract, and persist only allowed metadata and normalized records.</done>
</task>

<task type="auto">
  <name>Task 2: Implement duplicate blocking and the real earlier-batch inspection path</name>
  <files>src/preload/index.ts, src/shared/contracts/import.ts, src/main/import/duplicate-service.ts, src/main/import/parser.ts, src/main/ipc/import.ts, src/main/persistence/db.ts, tests/unit/import/duplicates.test.ts, tests/unit/import/persistence.test.ts</files>
  <read_first>.planning/phases/02-statement-import-pipeline/02-CONTEXT.md, .planning/phases/02-statement-import-pipeline/02-RESEARCH.md, src/main/security/pin-service.ts, src/main/persistence/db.ts, src/shared/contracts/import.ts, src/preload/index.ts, tests/fixtures/import/icici-valid.xlsx</read_first>
  <action>Create `src/main/import/duplicate-service.ts` and use `node:crypto` SHA-256 to compute both file fingerprints and normalized transaction signatures. File fingerprints must block re-imported files even after rename. Transaction signatures must include normalized date, optional value date, minor-unit amount, normalized narration, reference field when present, and running balance when present, while tolerating harmless formatting differences such as spacing and casing. Run duplicate checks during staging and confirm them again in the same transaction that persists import results. When a duplicate is blocked, persist the prior batch reference and expose `reasonTitle`, `reasonBody`, and `priorBatchId`. Add a real repository + main IPC + preload flow named `inspectPriorImportBatch` that returns the earlier batch summary/details needed by the renderer's `View earlier batch` action, including batch label, imported-at timestamp, file count, and duplicate-causing file reference, without expanding into full Phase 3 history browsing. Ensure valid files in a multi-file batch continue even when sibling files are rejected or duplicate-blocked.</action>
  <acceptance_criteria>`src/main/import/duplicate-service.ts` contains `createHash('sha256')` or equivalent SHA-256 logic; `src/main/ipc/import.ts` registers `inspectPriorImportBatch` and returns duplicate-blocked file states with `priorBatchId`; `src/preload/index.ts` exposes `inspectPriorImportBatch`; `tests/unit/import/duplicates.test.ts` covers renamed-file duplicate blocking and normalized transaction-signature duplicate blocking; `tests/unit/import/persistence.test.ts` proves valid files can still import when other files in the same batch are rejected or duplicate-blocked and that prior-batch inspection data can be loaded for a blocked duplicate.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/import/duplicates.test.ts tests/unit/import/persistence.test.ts</automated>
  </verify>
  <done>Duplicate detection is deterministic, explainable, and strong enough to block renamed or reformatted re-imports without collapsing the entire batch.</done>
</task>

</tasks>

<verification>
Run parser, duplicate, and persistence unit suites. Confirm all supported formats, worksheet ambiguity handling, record-only persistence, duplicate blocking, and earlier-batch inspection work from the main-process import boundary.
</verification>

<success_criteria>
Phase 2 has a trustworthy core import engine: it stages files, parses supported ICICI exports, rejects unsupported variants clearly, persists only allowed metadata, and blocks clear duplicates with a real inspectable earlier-batch path.
</success_criteria>

<output>
After completion, create `.planning/phases/02-statement-import-pipeline/02-02-SUMMARY.md`
</output>
