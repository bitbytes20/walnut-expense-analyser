---
phase: 02-statement-import-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/renderer/App.tsx
  - src/renderer/mockWalnutApi.ts
  - src/renderer/features/dashboard/EmptyDashboard.tsx
  - src/renderer/features/import/ImportWorkspace.tsx
  - src/renderer/features/import/StagedFileRow.tsx
  - src/renderer/features/import/WorksheetChoicePanel.tsx
  - src/renderer/features/import/ReasonPanel.tsx
  - src/renderer/features/import/GuidanceCard.tsx
  - src/renderer/features/import/ImportSummaryGroup.tsx
  - tests/e2e/import-flow.spec.ts
autonomous: true
requirements:
  - IMPT-01
  - IMPT-02
  - IMPT-04
must_haves:
  truths:
    - "The owner can launch the same import workspace from the dashboard empty state and a dedicated Import Statements screen."
    - "A staged batch keeps ready, sheet-choice, rejected, and duplicate-blocked files visible and individually understandable."
    - "Import results stay inside the workspace and show imported counts, rejection reasons, duplicate earlier-batch links, and privacy messaging."
  artifacts:
    - path: "src/renderer/features/import/ImportWorkspace.tsx"
      provides: "The staged import flow and summary workspace inside the shell"
      contains: "Select files"
    - path: "src/renderer/features/import/StagedFileRow.tsx"
      provides: "Per-file row card with status-specific actions"
      contains: "View earlier batch"
    - path: "tests/e2e/import-flow.spec.ts"
      provides: "End-to-end renderer flow coverage for staging, sheet choice, summary, and duplicate inspection"
      contains: "Import statements"
  key_links:
    - from: "src/renderer/App.tsx"
      to: "src/renderer/features/import/ImportWorkspace.tsx"
      via: "Dedicated import screen route and empty-dashboard CTA handoff"
      pattern: "import-statements"
    - from: "src/renderer/features/import/ImportWorkspace.tsx"
      to: "window.walnut"
      via: "Stage, sheet choice, remove, commit, and earlier-batch inspection actions"
      pattern: "stageImportFiles|chooseImportSheet|commitImportBatch"
    - from: "src/renderer/mockWalnutApi.ts"
      to: "tests/e2e/import-flow.spec.ts"
      via: "Mock import workspace behavior for browser-driven verification"
      pattern: "duplicate-blocked|needs-sheet-selection|rejected"
---

<objective>
Build the renderer-side import workspace and summary flow so Phase 2 presents a staged, investigable import experience instead of a raw utility dialog.

Purpose: Satisfy the UI contract and make the import pipeline understandable and trustworthy from the owner’s perspective.
Output: Import route/screen wiring, staged batch components, worksheet/reason panels, summary view, mock API support, and e2e flow coverage.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-statement-import-pipeline/02-CONTEXT.md
@.planning/phases/02-statement-import-pipeline/02-UI-SPEC.md
@.planning/phases/02-statement-import-pipeline/02-VALIDATION.md
@.planning/phases/02-statement-import-pipeline/02-01-PLAN.md
@.planning/phases/01-product-shell-and-security/01-02-SUMMARY.md

<interfaces>
From src/renderer/features/app-shell/AppShell.tsx:
```typescript
interface AppShellProps extends PropsWithChildren {
  title: string;
  eyebrow: string;
  actions?: ReactNode;
}
```

From src/shared/contracts/import.ts:
```typescript
export interface ImportStageFile {
  id: string;
  fileName: string;
  status: "ready" | "needs-sheet-selection" | "rejected" | "duplicate-blocked";
  sheetOptions?: ImportSheetOption[];
  priorBatch?: PriorImportBatchReference;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route both entry points into one staged import workspace</name>
  <files>src/renderer/App.tsx, src/renderer/features/dashboard/EmptyDashboard.tsx, src/renderer/features/import/ImportWorkspace.tsx, src/renderer/features/import/GuidanceCard.tsx</files>
  <read_first>src/renderer/App.tsx, src/renderer/features/dashboard/EmptyDashboard.tsx, src/renderer/features/app-shell/AppShell.tsx, src/renderer/features/import/ImportWorkspace.tsx, .planning/phases/02-statement-import-pipeline/02-CONTEXT.md, .planning/phases/02-statement-import-pipeline/02-UI-SPEC.md</read_first>
  <action>Keep `currentView` as `dashboard` for app-shell routing, but add a local dashboard screen state with the exact route key `import-statements` so the shell can render either the existing overview surface or the dedicated import workspace. Update `src/renderer/App.tsx` so the empty-dashboard CTA and a dedicated shell action both navigate to the same `ImportWorkspace` component per D-01. In `EmptyDashboard.tsx`, keep the exact heading `Ready for your first import` but update the body and primary CTA copy to the approved Phase 2 text `Bring in your ICICI statements` and `Import statements` where appropriate. In `ImportWorkspace.tsx`, implement the two-zone layout from the UI contract with a max content width of `1180px`, left task column max `760px`, right guidance column max `320px`, and the persistent helper line `Statement files are read for import and are not stored by Walnut.`</action>
  <acceptance_criteria>`src/renderer/App.tsx` contains the exact route key `import-statements`; the file renders `ImportWorkspace`; `src/renderer/features/dashboard/EmptyDashboard.tsx` contains the exact button label `Import statements`; `src/renderer/features/import/GuidanceCard.tsx` contains the exact privacy line `Statement files are read for import and are not stored by Walnut.`; `ImportWorkspace.tsx` contains the exact section heading `Select files`.</acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/import-flow.spec.ts --grep "entry points"</automated>
  </verify>
  <done>Both required entry points land in one workspace, and the workspace presents the approved structure and privacy framing.</done>
</task>

<task type="auto">
  <name>Task 2: Implement staged file rows, worksheet choice, reason panels, and summary groups</name>
  <files>src/renderer/mockWalnutApi.ts, src/renderer/features/import/ImportWorkspace.tsx, src/renderer/features/import/StagedFileRow.tsx, src/renderer/features/import/WorksheetChoicePanel.tsx, src/renderer/features/import/ReasonPanel.tsx, src/renderer/features/import/ImportSummaryGroup.tsx, tests/e2e/import-flow.spec.ts</files>
  <read_first>src/renderer/mockWalnutApi.ts, src/renderer/features/import/ImportWorkspace.tsx, src/renderer/features/import/StagedFileRow.tsx, tests/e2e/import-flow.spec.ts, src/shared/contracts/import.ts, .planning/phases/02-statement-import-pipeline/02-CONTEXT.md, .planning/phases/02-statement-import-pipeline/02-UI-SPEC.md</read_first>
  <action>Extend `src/renderer/mockWalnutApi.ts` with mock implementations for `stageImportFiles`, `chooseImportSheet`, `removeStagedFile`, `commitImportBatch`, and `inspectEarlierBatch` using realistic mixed batch data. In the import feature components, render plain-language sections in the exact staged sequence `Select files`, `Review staged files`, `Resolve sheet choice if needed`, and `Import summary`. Each staged file row must show filename, file type badge, status, and one context-specific action: `Review sheet`, `View reason`, `View earlier batch`, or `Remove`. Keep rejected and duplicate-blocked rows visible. Build worksheet choice as an inline expansion or side panel, mark the recommended sheet with the exact label `Recommended`, and close it on `Esc`. Build the summary screen inside the workspace with grouped result headers `Imported`, `Rejected`, and `Duplicate blocked`; include imported file counts, transaction counts, inline lazy account-creation confirmation when the first import creates the account, and prior batch inspection details instead of sending the owner away to a history screen.</action>
  <acceptance_criteria>`src/renderer/mockWalnutApi.ts` contains the exact mocked statuses `ready`, `needs-sheet-selection`, `rejected`, and `duplicate-blocked`; `src/renderer/features/import/StagedFileRow.tsx` contains the exact action labels `Review sheet`, `View reason`, `View earlier batch`, and `Remove`; `src/renderer/features/import/WorksheetChoicePanel.tsx` contains the exact label `Recommended`; `src/renderer/features/import/ImportSummaryGroup.tsx` contains the exact group headers `Imported`, `Rejected`, and `Duplicate blocked`; `tests/e2e/import-flow.spec.ts` asserts the staged sequence and summary groups.</acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/e2e/import-flow.spec.ts</automated>
  </verify>
  <done>The renderer provides the full staged import experience, including sheet resolution, explicit rejection and duplicate details, and in-workspace summary results.</done>
</task>

</tasks>

<verification>
Run the import-flow e2e suite. Confirm both entry points work, staged rows remain readable while mixed statuses exist, worksheet choice is explicit, and the summary stays inside the same workspace even when every file fails.
</verification>

<success_criteria>
The owner can use a single premium-feeling import workspace to stage files, inspect issues, resolve sheet ambiguity, and review results without losing trust in what Walnut did.
</success_criteria>

<output>
After completion, create `.planning/phases/02-statement-import-pipeline/02-03-SUMMARY.md`
</output>
