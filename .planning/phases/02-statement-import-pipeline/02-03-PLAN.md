---
phase: 02-statement-import-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/renderer/App.tsx
  - src/renderer/mockWalnutApi.ts
  - src/renderer/features/dashboard/EmptyDashboard.tsx
  - src/renderer/features/import/ImportWorkspace.tsx
  - src/renderer/features/import/ImportWorkspaceHeader.tsx
  - src/renderer/features/import/StagedFileRow.tsx
  - src/renderer/features/import/WorksheetChoicePanel.tsx
  - src/renderer/features/import/ReasonPanel.tsx
  - src/renderer/features/import/ImportSummary.tsx
  - src/renderer/features/app-shell/AppShell.tsx
  - tests/unit/import-workspace.test.tsx
  - tests/e2e/import-flow.spec.ts
autonomous: true
requirements:
  - IMPT-01
  - IMPT-02
  - IMPT-03
  - IMPT-04
must_haves:
  truths:
    - "The import flow feels staged and trustworthy rather than opaque or immediate."
    - "Dashboard and Import Statements entry points land in one shared import workspace."
    - "The renderer keeps rejected, duplicate-blocked, and imported files visible with investigable reasons and prior-batch inspection."
  artifacts:
    - path: "src/renderer/features/import/ImportWorkspace.tsx"
      provides: "Shared import workspace with two-zone layout and staged sequence"
      contains: "Import statements"
    - path: "src/renderer/features/import/StagedFileRow.tsx"
      provides: "Per-file status cards with actions for review sheet, reason, earlier batch, and remove"
      contains: "View earlier batch"
    - path: "tests/e2e/import-flow.spec.ts"
      provides: "End-to-end staged import coverage"
      contains: "Bring in your ICICI statements"
  key_links:
    - from: "src/renderer/features/dashboard/EmptyDashboard.tsx"
      to: "src/renderer/features/import/ImportWorkspace.tsx"
      via: "Shared import entry route"
      pattern: "Import statements"
    - from: "src/renderer/mockWalnutApi.ts"
      to: "src/shared/contracts/import.ts"
      via: "Mock import API parity with main-process contract"
      pattern: "duplicate-blocked|needs-sheet-selection"
    - from: "src/renderer/features/import/ReasonPanel.tsx"
      to: "src/main/ipc/import.ts"
      via: "Reason and prior-batch inspection path"
      pattern: "priorBatchId"
---

<objective>
Implement the Phase 2 renderer experience: one shared import workspace, staged file review, worksheet choice UI, rejection/duplicate reasoning, and final import summary.

Purpose: Expose the main-process import engine through a premium, trust-first desktop workflow that matches the approved UI contract.
Output: Dashboard/import-screen entry points, a staged batch workspace, worksheet choice and reason panels, and e2e coverage for the full import flow.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-statement-import-pipeline/02-CONTEXT.md
@.planning/phases/02-statement-import-pipeline/02-RESEARCH.md
@.planning/phases/02-statement-import-pipeline/02-UI-SPEC.md
@.planning/phases/02-statement-import-pipeline/02-VALIDATION.md
@.planning/phases/02-statement-import-pipeline/02-01-PLAN.md
@.planning/phases/02-statement-import-pipeline/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared import workspace and staged file-review UI</name>
  <files>src/renderer/App.tsx, src/renderer/mockWalnutApi.ts, src/renderer/features/dashboard/EmptyDashboard.tsx, src/renderer/features/import/ImportWorkspace.tsx, src/renderer/features/import/ImportWorkspaceHeader.tsx, src/renderer/features/import/StagedFileRow.tsx, src/renderer/features/import/WorksheetChoicePanel.tsx, src/renderer/features/import/ReasonPanel.tsx, src/renderer/features/app-shell/AppShell.tsx, tests/unit/import-workspace.test.tsx</files>
  <read_first>.planning/phases/02-statement-import-pipeline/02-CONTEXT.md, .planning/phases/02-statement-import-pipeline/02-UI-SPEC.md, src/renderer/App.tsx, src/renderer/features/dashboard/EmptyDashboard.tsx, src/renderer/features/app-shell/AppShell.tsx, src/renderer/mockWalnutApi.ts, src/shared/contracts/import.ts, tests/unit/setup.ts</read_first>
  <action>Extend the renderer so both the empty dashboard CTA and a dedicated Import Statements route/entry land in one shared `ImportWorkspace`. Implement the exact staged sequence from the UI contract: `Select files -> Review staged files -> Resolve sheet choice if needed -> Import summary`. Build a two-zone layout inside the existing shell, with the active task column on the left and a guidance column on the right containing supported-format guidance and the exact privacy line `Statement files are read for import and are not stored by Walnut.` Create full-width staged file cards showing filename, file type badge, detectable account label, detectable statement period, status badge, and a context-specific action: `Review sheet`, `View reason`, `View earlier batch`, or `Remove`. Keep `Rejected` and `Duplicate blocked` rows visible in the batch instead of removing them. Add an in-flow worksheet choice panel with `Recommended` indication and an inline/side-panel reason viewer for rejected and duplicate files. Implement the approved keyboard contract in this task: `Tab` and `Shift+Tab` traverse staged rows and row actions in visual order, `Esc` closes worksheet or reason panels, the file picker trigger is keyboard reachable from both entry points, and focus moves to the staging heading after file selection. Mirror the real IPC contract in `src/renderer/mockWalnutApi.ts` so browser-harness development can exercise ready, rejected, duplicate-blocked, and worksheet-ambiguity states. Create `tests/unit/import-workspace.test.tsx` as a fast renderer/integration smoke test that verifies workspace rendering, action labels, keyboard reachability, `Esc` panel close behavior, and focus transfer to the staging heading after file selection.</action>
  <acceptance_criteria>`src/renderer/features/import/ImportWorkspace.tsx` exists and contains `Import statements`; `EmptyDashboard.tsx` uses the copy `Bring in your ICICI statements`; `StagedFileRow.tsx` contains `Review sheet`, `View reason`, `View earlier batch`, and `Remove`; `WorksheetChoicePanel.tsx` contains `Recommended`; `ReasonPanel.tsx` contains the privacy line `Statement files are read for import and are not stored by Walnut.` or the exact rejection/duplicate reason fields from the contract; `tests/unit/import-workspace.test.tsx` verifies keyboard reachability, `Esc` panel close behavior, and focus transfer to the staging heading after file selection.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/import-workspace.test.tsx</automated>
  </verify>
  <done>The renderer exposes a single premium import workspace that makes every staged file independently understandable and actionable before import commit.</done>
</task>

<task type="auto">
  <name>Task 2: Add import summary, lazy-account confirmation, and end-to-end import flow coverage</name>
  <files>src/renderer/features/import/ImportSummary.tsx, src/renderer/features/import/ImportWorkspace.tsx, src/renderer/mockWalnutApi.ts, tests/e2e/import-flow.spec.ts</files>
  <read_first>.planning/phases/02-statement-import-pipeline/02-CONTEXT.md, .planning/phases/02-statement-import-pipeline/02-UI-SPEC.md, src/renderer/features/import/ImportWorkspace.tsx, src/renderer/mockWalnutApi.ts, src/shared/contracts/account.ts, src/shared/contracts/import.ts, tests/unit/import-workspace.test.tsx</read_first>
  <action>After batch commit, render an `ImportSummary` state inside the same workspace with grouped sections for `Imported`, `Rejected`, and `Duplicate blocked`. Show counts for files imported and transactions created. If the first successful import lazily creates the ICICI account profile, show that confirmation inline in the summary header rather than routing away. If every selected file fails, still render the summary screen with clear reasons instead of dropping the owner back to the dashboard. In the mock API and the real renderer flow, preserve a minimal inspect path for the earlier duplicate batch inside the workspace, such as a detail drawer or panel, without expanding into full Phase 3 history browsing. Create `tests/e2e/import-flow.spec.ts` in this task and add Playwright coverage for dashboard entry, dedicated import entry, worksheet selection, duplicate-blocked messaging with `View earlier batch`, all-failed summary behavior, `Esc` panel close behavior, keyboard-reachable file picker access, and focus transfer after file choice.</action>
  <acceptance_criteria>`ImportSummary.tsx` exists and contains the headings `Imported`, `Rejected`, and `Duplicate blocked`; the summary header can render a first-import account confirmation; `tests/e2e/import-flow.spec.ts` covers dashboard entry and dedicated import entry plus worksheet, duplicate, all-failed, `Esc`, file-picker keyboard access, and focus-transfer flows; the renderer does not route back to the dashboard automatically when all selected files fail.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/import-workspace.test.tsx && npx playwright test tests/e2e/import-flow.spec.ts</automated>
  </verify>
  <done>The import flow ends in a trustworthy result screen that explains exactly what happened, preserves investigable duplicate context, and confirms the lazy account-creation path when it occurs.</done>
</task>

</tasks>

<verification>
Run the import e2e suite and the full unit suite. Confirm both import entry points work, staged states stay visible, worksheet ambiguity is resolvable, duplicate blocks expose prior-batch inspection, and summaries render correctly even when every file fails.
</verification>

<success_criteria>
Phase 2 delivers the full trusted import loop for known ICICI statements: files can be staged, validated, imported, or blocked with clear explanations from a single consistent workspace.
</success_criteria>

<output>
After completion, create `.planning/phases/02-statement-import-pipeline/02-03-SUMMARY.md`
</output>
