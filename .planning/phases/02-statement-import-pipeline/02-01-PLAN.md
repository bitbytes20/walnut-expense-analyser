---
phase: 02-statement-import-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/shared/contracts/app-state.ts
  - src/shared/contracts/import.ts
  - src/preload/index.ts
  - sample_files/OpTransactionHistory-2016.xls
  - sample_files/OpTransactionHistory-2017.xls
  - tests/fixtures/import/icici-valid.csv
  - tests/fixtures/import/icici-valid.xls
  - tests/fixtures/import/icici-valid.xlsx
  - tests/fixtures/import/icici-unsupported-variant.xlsx
  - tests/unit/import/parser.test.ts
  - tests/unit/import/rejections.test.ts
autonomous: true
requirements:
  - IMPT-01
  - IMPT-02
must_haves:
  truths:
    - "Supported ICICI CSV, XLS, and XLSX files can be staged against real fixtures instead of guessed sample data."
    - "Unsupported or malformed statement variants can be rejected with explicit, testable reasons."
    - "The import flow can expose one consistent staged-file model to the owner before parser and UI details branch apart."
  artifacts:
    - path: "sample_files/OpTransactionHistory-2016.xls"
      provides: "Real ICICI source statement used to derive committed import fixtures"
      contains: "OpTransactionHistory"
    - path: "src/shared/contracts/import.ts"
      provides: "Typed preload-safe contracts for staged files, worksheet choice, import summaries, and duplicate/rejection states"
      contains: "StagedImportFile"
    - path: "tests/fixtures/import/icici-valid.csv"
      provides: "Golden fixture for CSV parsing and duplicate checks"
      contains: "Date"
  key_links:
    - from: "src/shared/contracts/import.ts"
      to: "src/preload/index.ts"
      via: "Typed import bridge surface"
      pattern: "stageImportFiles|chooseImportSheet|commitImportBatch"
    - from: "tests/unit/import/parser.test.ts"
      to: "sample_files/OpTransactionHistory-2016.xls"
      via: "Real-source-to-fixture parser assertions"
      pattern: "OpTransactionHistory"
---

<objective>
Create the import foundation for Phase 2: fixture assets, shared contracts, schema extensions, and the IPC surface that later parsing and renderer work will build against.

Purpose: Prevent parser and UI work from inventing file shapes, metadata contracts, or persistence layout ad hoc.
Output: Real-source-derived import fixtures, import-focused shared contracts, and a typed preload-facing import surface for later waves.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-statement-import-pipeline/02-CONTEXT.md
@.planning/phases/02-statement-import-pipeline/02-RESEARCH.md
@.planning/phases/02-statement-import-pipeline/02-UI-SPEC.md
@.planning/phases/02-statement-import-pipeline/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add import fixtures, parser dependencies, and shared import contracts</name>
  <files>package.json, src/shared/contracts/import.ts, src/shared/contracts/app-state.ts, src/preload/index.ts, tests/fixtures/import/icici-valid.csv, tests/fixtures/import/icici-valid.xls, tests/fixtures/import/icici-valid.xlsx, tests/fixtures/import/icici-unsupported-variant.xlsx, tests/unit/import/parser.test.ts, tests/unit/import/rejections.test.ts</files>
  <read_first>.planning/phases/02-statement-import-pipeline/02-CONTEXT.md, .planning/phases/02-statement-import-pipeline/02-RESEARCH.md, .planning/phases/02-statement-import-pipeline/02-UI-SPEC.md, package.json, src/shared/contracts/app-state.ts, src/preload/index.ts, sample_files/OpTransactionHistory-2016.xls, sample_files/OpTransactionHistory-2017.xls</read_first>
  <action>Install the exact import stack `xlsx`, `date-fns`, and `zod` in `package.json`. Create `src/shared/contracts/import.ts` and define the Phase 2 import contract surface with exact staged states `ready`, `needs-sheet-selection`, `rejected`, `duplicate-blocked`, `processing`, and `imported`; worksheet candidate shape with `recommended: boolean`; per-file status details including explicit `reasonCode`, `reasonTitle`, `reasonBody`, and prior-batch reference fields; normalized import row fields including `transactionDateRaw`, `valueDateRaw`, `rawNarration`, `cleanedDescription`, `debitAmountMinor`, `creditAmountMinor`, `runningBalanceMinor`, `direction`, `reference`, `sourceFileId`, and `importBatchId`. Extend `src/shared/contracts/app-state.ts` and the preload API in `src/preload/index.ts` with `stageImportFiles`, `chooseImportSheet`, `removeStagedFile`, `commitImportBatch`, and `inspectPriorImportBatch`. Use the real source files `sample_files/OpTransactionHistory-2016.xls` and `sample_files/OpTransactionHistory-2017.xls` as the canonical ICICI inputs for Wave 1: inspect their sheet structure and headers, document the observed column/date/narration patterns in tests, and derive committed fixture files under `tests/fixtures/import/` for one known-good ICICI CSV, XLS, and XLSX plus one unsupported workbook variant. Create parser and rejection unit test files that load the committed fixtures and, where useful, assert the derivation aligns with the sample `.xls` source files.</action>
  <acceptance_criteria>`package.json` contains `xlsx`, `date-fns`, and `zod`; `src/shared/contracts/import.ts` contains the exact strings `duplicate-blocked`, `needs-sheet-selection`, and `cleanedDescription`; `src/preload/index.ts` contains the functions `stageImportFiles`, `chooseImportSheet`, `removeStagedFile`, `commitImportBatch`, and `inspectPriorImportBatch`; all four fixture files exist under `tests/fixtures/import/`; `tests/unit/import/parser.test.ts` or `tests/unit/import/rejections.test.ts` reference `OpTransactionHistory-2016.xls` or `OpTransactionHistory-2017.xls` as the real-source basis for the committed fixtures.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/import/parser.test.ts tests/unit/import/rejections.test.ts</automated>
  </verify>
  <done>The repo has real import fixtures, parser dependencies, and shared contracts that make Phase 2 implementation concrete instead of speculative.</done>
</task>

<task type="auto">
</tasks>

<verification>
Run the import parser and rejection unit tests. Confirm the contract layer and real fixture set exist before parser logic and renderer workflow work begin.
</verification>

<success_criteria>
Phase 2 has a solid import foundation: the real ICICI sample files are promoted into committed fixtures and shared contracts make the later parser and renderer waves concrete and testable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-statement-import-pipeline/02-01-SUMMARY.md`
</output>
