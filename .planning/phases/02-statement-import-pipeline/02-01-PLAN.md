---
phase: 02-statement-import-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/shared/contracts/import.ts
  - src/shared/contracts/app-state.ts
  - src/preload/index.ts
  - src/main/import/workbook-loader.ts
  - src/main/import/format-detection.ts
  - src/main/import/icici-parser.ts
  - tests/fixtures/import/icici-valid.csv
  - tests/fixtures/import/icici-valid.xls
  - tests/fixtures/import/icici-valid.xlsx
  - tests/fixtures/import/icici-unsupported-variant.xlsx
  - tests/unit/import/parser.test.ts
  - tests/unit/import/rejections.test.ts
autonomous: true
requirements:
  - IMPT-01
  - IMPT-02
must_haves:
  truths:
    - "Supported ICICI CSV, XLS, and XLSX files can be staged and recognized before any records are written."
    - "Unsupported or structurally invalid files surface explicit reject reasons and supported-format guidance."
    - "Excel files with more than one plausible worksheet pause for an owner choice instead of guessing."
  artifacts:
    - path: "src/shared/contracts/import.ts"
      provides: "Shared import batch, file, sheet-choice, and summary contracts"
      contains: "ImportStageFile"
    - path: "src/main/import/icici-parser.ts"
      provides: "ICICI-specific recognizable-column parsing and normalization"
      contains: "parseIciciStatement"
    - path: "tests/unit/import/parser.test.ts"
      provides: "Fixture-backed coverage for CSV, XLS, XLSX, and worksheet ambiguity"
      contains: "icici-valid"
  key_links:
    - from: "src/preload/index.ts"
      to: "src/shared/contracts/import.ts"
      via: "Typed preload bridge for stage/select/commit import actions"
      pattern: "stageImportFiles|chooseImportSheet|commitImportBatch"
    - from: "src/main/import/format-detection.ts"
      to: "src/main/import/icici-parser.ts"
      via: "Recognizable ICICI header detection and sheet scoring"
      pattern: "transaction date|narration|debit|credit"
    - from: "tests/fixtures/import/icici-valid.xlsx"
      to: "tests/unit/import/parser.test.ts"
      via: "Golden fixture assertions"
      pattern: "icici-valid.xlsx"
---

<objective>
Define the shared Phase 2 import contracts and implement the staging/parser foundation that can recognize trusted ICICI statements before persistence.

Purpose: Give both the backend and renderer a concrete import contract and eliminate guesswork by anchoring parsing behavior to real fixtures and tests.
Output: Shared import contracts, preload method surface, ICICI staging/parser modules, and fixture-backed parser/rejection tests.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-statement-import-pipeline/02-CONTEXT.md
@.planning/phases/02-statement-import-pipeline/02-RESEARCH.md
@.planning/phases/02-statement-import-pipeline/02-UI-SPEC.md
@.planning/phases/02-statement-import-pipeline/02-VALIDATION.md
@.planning/phases/01-product-shell-and-security/01-01-SUMMARY.md

<interfaces>
From src/shared/contracts/app-state.ts:
```typescript
export interface WalnutApi {
  loadAppState: () => Promise<AppShellState>;
  saveOnboardingProgress: (input: SaveOnboardingProgressInput) => Promise<AppShellState>;
  completeOnboarding: (input: CompleteOnboardingInput) => Promise<AppShellState>;
  lockNow: (reason?: LockReason) => Promise<AppShellState>;
  unlockWithPin: (pin: string) => Promise<UnlockResult>;
  beginRecoveryReset: (payload: RecoveryResetPayload) => Promise<AppShellState>;
  saveAccountProfile: (draft: AccountProfileDraft) => Promise<AppShellState>;
  copyRecoveryKeyAcknowledged: () => Promise<AppShellState>;
  downloadRecoveryKeyAcknowledged: () => Promise<AppShellState>;
  getSecurityEvents: () => Promise<SecurityEvent[]>;
  ping: () => Promise<string>;
}
```

From src/shared/contracts/account.ts:
```typescript
export interface AccountProfileDraft {
  bankName: "ICICI";
  displayName: string;
  accountHolderName: string;
  maskedAccountNumber?: string;
  nickname?: string;
  baseCurrency: string;
  openingBalance?: number;
  openingBalanceDate?: string;
  skippedDuringOnboarding?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add import contracts, preload methods, and dependency wiring</name>
  <files>package.json, src/shared/contracts/import.ts, src/shared/contracts/app-state.ts, src/preload/index.ts</files>
  <read_first>package.json, src/shared/contracts/app-state.ts, src/preload/index.ts, .planning/PROJECT.md, .planning/phases/02-statement-import-pipeline/02-CONTEXT.md, .planning/phases/02-statement-import-pipeline/02-RESEARCH.md, .planning/phases/02-statement-import-pipeline/02-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: The shared API surface exposes `stageImportFiles`, `chooseImportSheet`, `removeStagedFile`, `commitImportBatch`, and `inspectEarlierBatch`.
    - Test 2: Import stage contracts can represent `ready`, `needs-sheet-selection`, `rejected`, and `duplicate-blocked`.
    - Test 3: Import summary contracts include imported counts, reject reasons, duplicate prior-batch references, and lazy account-creation confirmation.
  </behavior>
  <action>Add `xlsx`, `date-fns`, and `zod` to `package.json` dependencies. Create `src/shared/contracts/import.ts` exporting `ImportStageStatus = 'ready' | 'needs-sheet-selection' | 'rejected' | 'duplicate-blocked'`, `ImportSheetOption`, `ImportStageFile`, `ImportBatchStage`, `ImportBatchCommitResult`, `ImportRejectReasonCode`, and `PriorImportBatchReference`. Extend `src/shared/contracts/app-state.ts` so `WalnutApi` includes `stageImportFiles(): Promise<ImportBatchStage>`, `chooseImportSheet(input: { batchId: string; fileId: string; sheetName: string }): Promise<ImportBatchStage>`, `removeStagedFile(input: { batchId: string; fileId: string }): Promise<ImportBatchStage>`, `commitImportBatch(input: { batchId: string }): Promise<ImportBatchCommitResult>`, and `inspectEarlierBatch(input: { batchId: string }): Promise<PriorImportBatchReference | undefined>`. Update `src/preload/index.ts` to expose the exact IPC channel names `import:stage-files`, `import:choose-sheet`, `import:remove-staged-file`, `import:commit-batch`, and `import:inspect-earlier-batch`. Keep all file access in main process and do not add renderer `FileReader`, `showOpenFilePicker`, or direct filesystem APIs.</action>
  <acceptance_criteria>`package.json` contains the strings `"xlsx"`, `"date-fns"`, and `"zod"`; `src/shared/contracts/import.ts` contains the exact statuses `ready`, `needs-sheet-selection`, `rejected`, and `duplicate-blocked`; `src/shared/contracts/app-state.ts` contains the exact API method names `stageImportFiles`, `chooseImportSheet`, `removeStagedFile`, `commitImportBatch`, and `inspectEarlierBatch`; `src/preload/index.ts` contains the exact IPC channels `import:stage-files` and `import:commit-batch`.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/import/parser.test.ts tests/unit/import/rejections.test.ts</automated>
  </verify>
  <done>Downstream plans can build against a stable import API surface without redefining statuses, payloads, or channel names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build fixture-backed workbook loading and ICICI staging detection</name>
  <files>src/main/import/workbook-loader.ts, src/main/import/format-detection.ts, src/main/import/icici-parser.ts, tests/fixtures/import/icici-valid.csv, tests/fixtures/import/icici-valid.xls, tests/fixtures/import/icici-valid.xlsx, tests/fixtures/import/icici-unsupported-variant.xlsx, tests/unit/import/parser.test.ts</files>
  <read_first>src/main/import/workbook-loader.ts, src/main/import/format-detection.ts, src/main/import/icici-parser.ts, tests/unit/import/parser.test.ts, .planning/phases/02-statement-import-pipeline/02-CONTEXT.md, .planning/phases/02-statement-import-pipeline/02-RESEARCH.md, .planning/phases/02-statement-import-pipeline/02-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Known-good CSV, XLS, and XLSX fixtures produce staged files with `ready` status and normalized required columns.
    - Test 2: A workbook with multiple plausible ICICI sheets produces `needs-sheet-selection` plus a `recommended` option.
    - Test 3: Parsed rows preserve raw statement fields and derive only `direction`.
  </behavior>
  <action>Create real fixture files at `tests/fixtures/import/icici-valid.csv`, `tests/fixtures/import/icici-valid.xls`, `tests/fixtures/import/icici-valid.xlsx`, and `tests/fixtures/import/icici-unsupported-variant.xlsx`. In `src/main/import/workbook-loader.ts`, read CSV/XLS/XLSX via `xlsx` from the main process and return sheet names plus row matrices without persisting source bytes. In `src/main/import/format-detection.ts`, normalize header text by trimming whitespace, lowercasing, and collapsing punctuation so recognizable ICICI columns can match. Score sheets using the required fields `transaction date`, `narration`, and either `debit amount` or `credit amount`; when more than one sheet scores plausibly, return `needs-sheet-selection` with a `recommended` sheet. In `src/main/import/icici-parser.ts`, export `parseIciciStatement` and `cleanDisplayDescription` that preserve `transactionDateRaw`, `valueDateRaw`, `rawNarration`, `cleanedDescription`, `debitAmountMinor`, `creditAmountMinor`, `runningBalanceMinor`, `statementReference`, `sourceFileId`, and `importBatchId`, while deriving only `direction: 'debit' | 'credit'`. Represent money in integer paise, not floats.</action>
  <acceptance_criteria>`src/main/import/icici-parser.ts` contains the exported names `parseIciciStatement` and `cleanDisplayDescription`; the file contains the exact raw-field names `transactionDateRaw`, `valueDateRaw`, `rawNarration`, `cleanedDescription`, `debitAmountMinor`, `creditAmountMinor`, `runningBalanceMinor`, `sourceFileId`, and `importBatchId`; `tests/unit/import/parser.test.ts` references `icici-valid.csv`, `icici-valid.xls`, and `icici-valid.xlsx`; the parser code contains the exact string `needs-sheet-selection`.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/import/parser.test.ts</automated>
  </verify>
  <done>The import pipeline can stage and parse known ICICI files across all required formats and stop for explicit worksheet selection when necessary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement explicit rejection reasons and supported-format guidance</name>
  <files>src/main/import/format-detection.ts, src/main/import/icici-parser.ts, tests/unit/import/rejections.test.ts</files>
  <read_first>src/main/import/format-detection.ts, src/main/import/icici-parser.ts, tests/unit/import/rejections.test.ts, .planning/phases/02-statement-import-pipeline/02-CONTEXT.md, .planning/phases/02-statement-import-pipeline/02-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: Unsupported fixtures reject with a stable reason code and a human-readable guidance message.
    - Test 2: Partial multi-file staging marks only the invalid file as `rejected` while leaving valid files actionable.
    - Test 3: Balance continuity issues become warnings labeled `check-after-import` instead of hard failures.
  </behavior>
  <action>In the format-detection and parser modules, add stable reject reasons `missing-required-columns`, `unsupported-sheet-layout`, `unparseable-date`, and `empty-statement`. Attach the exact supported-format guidance sentence `Use an ICICI account statement in CSV, XLS, or XLSX with transaction date, narration, and debit or credit columns.` to rejected file results. Return per-file warnings array entries for soft balance continuity issues with the exact warning code `check-after-import`; do not throw or block commit for those warnings. Ensure multi-file staging returns a mixed batch containing both `ready` and `rejected` files instead of aborting the entire batch.</action>
  <acceptance_criteria>`tests/unit/import/rejections.test.ts` contains the exact guidance sentence `Use an ICICI account statement in CSV, XLS, or XLSX with transaction date, narration, and debit or credit columns.`; parser or detection code contains the exact reject reason strings `missing-required-columns` and `unsupported-sheet-layout`; parser or detection code contains the exact warning code `check-after-import`; rejection tests assert mixed ready and rejected files in one batch.</acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/import/rejections.test.ts</automated>
  </verify>
  <done>Unsupported variants fail clearly and specifically, while valid files in the same staged batch remain eligible to continue.</done>
</task>

</tasks>

<verification>
Run the parser and rejection suites. Confirm all three required formats stage successfully, unsupported variants reject with exact guidance, and worksheet ambiguity returns an explicit choice state.
</verification>

<success_criteria>
Phase 2 has a concrete import contract and a fixture-backed staging/parser foundation that satisfies IMPT-01 and IMPT-02 without touching persistence yet.
</success_criteria>

<output>
After completion, create `.planning/phases/02-statement-import-pipeline/02-01-SUMMARY.md`
</output>
