---
phase: 03-review-queue-and-import-history
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/shared/contracts/app-state.ts
  - src/shared/contracts/import.ts
  - src/preload/index.ts
  - src/main/persistence/db.ts
  - src/main/ipc/import.ts
  - tests/unit/import/review-mutations.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Review actions are batch-first, unresolved-only, auditable, and reversible."
    - "Restore semantics are persisted in storage rather than depending on ephemeral UI state."
    - "Queue-facing mutation APIs are ready for renderer re-entry work without carrying UI ownership in this wave."
  artifacts:
    - path: "src/main/persistence/db.ts"
      provides: "Atomic review resolution and soft-restore transactions with audit emission"
      contains: "resolveReviewItems"
  key_links:
    - from: "src/main/persistence/db.ts"
      to: "src/main/ipc/import.ts"
      via: "refetch-after-mutation queue and counter refresh plumbing"
      pattern: "resolveReviewItems|restoreReviewItems"
---

<objective>
Implement the auditable and reversible review-mutation backend that the dedicated queue UI can safely build on.

Purpose: Deliver the persistence and IPC mutation layer for Phase 3 without taking ownership of the renderer queue experience.
Output: Review mutation repository methods, shared API contracts, persisted undo or soft-restore, and unit coverage for resolution and restore flows.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-review-queue-and-import-history/03-CONTEXT.md
@.planning/phases/03-review-queue-and-import-history/03-RESEARCH.md
@.planning/phases/03-review-queue-and-import-history/03-UI-SPEC.md
@.planning/phases/03-review-queue-and-import-history/03-VALIDATION.md
@.planning/phases/03-review-queue-and-import-history/03-01-PLAN.md
@.planning/phases/03-review-queue-and-import-history/03-02-PLAN.md
@src/shared/contracts/import.ts
@src/main/persistence/db.ts
@src/shared/contracts/app-state.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement atomic review resolution, restore, and audit-backed mutation flows</name>
  <files>src/shared/contracts/app-state.ts, src/shared/contracts/import.ts, src/preload/index.ts, src/main/persistence/db.ts, src/main/ipc/import.ts, tests/unit/import/review-mutations.test.ts</files>
  <read_first>.planning/phases/03-review-queue-and-import-history/03-CONTEXT.md, .planning/phases/03-review-queue-and-import-history/03-RESEARCH.md, src/shared/contracts/app-state.ts, src/shared/contracts/import.ts, src/main/persistence/db.ts, src/main/ipc/import.ts, tests/unit/import/history-repository.test.ts</read_first>
  <behavior>
    - Test 1: single-item and bulk actions support the locked action set from D-03 and D-15, and only unresolved items are returned to the queue per D-04.
    - Test 2: edit-before-accept allows date, description, reference ID, and tags only, while amount and running balance stay protected and unchanged per D-13 and D-14.
    - Test 3: each resolution writes an audit event and can be soft-restored later for mistaken discard or duplicate decisions per D-16 and D-17.
  </behavior>
  <action>Extend the shared contracts with resolution-input, bulk-action, and restore payloads, and update `src/shared/contracts/app-state.ts` so the canonical `WalnutApi` owns queue reads, resolution, and restore methods. In `src/main/persistence/db.ts`, implement atomic `resolveReviewItems` and `restoreReviewItems` transactions that update review-item state, apply any allowed transaction changes, maintain unresolved counters, update attempt or batch status from `needs-review` to `imported` when the last unresolved item clears, and emit an audit row in the same transaction per D-16. Support the exact action set from D-03 and D-15 only: mark duplicate, mark not duplicate, accept as-is, edit before accept, discard, and apply tag, with the bulk subset limited exactly to the approved Phase 3 set. Persist soft-restore metadata instead of toast-only rollback so decisions survive relaunch per D-17. Register IPC/preload methods for queue reads, mutations, and restore, and make sure all queue results stay unresolved-only per D-04.</action>
  <acceptance_criteria>`src/main/persistence/db.ts` contains `resolveReviewItems` and `restoreReviewItems`; `src/shared/contracts/app-state.ts` includes the matching queue and restore API methods; mutation code rejects attempts to change amount or running balance during review; audit events are written for every resolution action; restoring a discarded or duplicate-marked item returns it to pending review state; `tests/unit/import/review-mutations.test.ts` covers single-item actions, bulk actions, protected fields, audit emission, and restore flows.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/import/review-mutations.test.ts</automated>
  </verify>
  <done>The backend now supports real review work across sessions with reversible, auditable, rule-bound mutations instead of ephemeral UI-only state.</done>
</task>

</tasks>

<verification>
Run the review-mutations unit suite. Confirm resolution work is atomic, auditable, and restorable before the renderer queue wiring begins.
</verification>

<success_criteria>
Phase 3 has a trustworthy mutation backend: review decisions are reversible, every action is audited, protected fields stay locked, and queue state remains authoritative across relaunches.
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-queue-and-import-history/03-03-SUMMARY.md`
</output>
