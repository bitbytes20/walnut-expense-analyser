---
phase: 03-review-queue-and-import-history
plan: 04
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - src/renderer/App.tsx
  - src/renderer/mockWalnutApi.ts
  - src/renderer/features/import/ImportHistoryScreen.tsx
  - src/renderer/features/import/ImportBatchDetailScreen.tsx
  - src/renderer/features/import/ReviewQueueScreen.tsx
  - src/renderer/features/import/ReviewBatchGroup.tsx
  - src/renderer/features/import/ReviewItemCard.tsx
  - src/renderer/features/import/ReviewDetailPanel.tsx
  - src/renderer/features/import/ReviewBulkActionBar.tsx
  - src/renderer/features/import/ReviewRestoreBanner.tsx
  - tests/unit/review-queue.test.tsx
  - tests/e2e/review-queue.spec.ts
autonomous: true
requirements:
  - REVW-01
must_haves:
  truths:
    - "Users can reopen unresolved batches into a dedicated review queue at any later time."
    - "Review actions stay batch-first, unresolved-only, and visibly tied back to their source import context."
    - "Queue, batch detail, and history counts refresh from authoritative persisted state after every mutation."
  artifacts:
    - path: "src/renderer/features/import/ReviewQueueScreen.tsx"
      provides: "Dedicated batch-grouped unresolved review queue with re-entry, bulk actions, and protected-field editing rules"
      contains: "No unresolved review items"
    - path: "src/renderer/features/import/ReviewRestoreBanner.tsx"
      provides: "Visible persisted restore affordance for destructive actions"
      contains: "Restore"
    - path: "tests/e2e/review-queue.spec.ts"
      provides: "End-to-end regression coverage for reopening, partial review, undo, and live counter refresh"
      contains: "Open review queue"
  key_links:
    - from: "src/renderer/features/import/ImportHistoryScreen.tsx"
      to: "src/renderer/features/import/ReviewQueueScreen.tsx"
      via: "reopen unresolved batch into batch-scoped queue"
      pattern: "Open review queue"
    - from: "src/renderer/features/import/ImportBatchDetailScreen.tsx"
      to: "src/renderer/features/import/ReviewQueueScreen.tsx"
      via: "direct batch-detail re-entry into unresolved review"
      pattern: "Review unresolved items"
---

<objective>
Finish Phase 3 with the dedicated review queue UI that can be reopened from history or batch detail, supports safe resolution work across sessions, and surfaces persisted restore flows clearly.

Purpose: Deliver the owner-facing triage workflow that turns persisted ambiguity into a manageable financial operations inbox instead of a dead-end import warning.
Output: Batch-scoped review queue UI, restore affordances, and end-to-end coverage for reopen and partial-progress flows.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-review-queue-and-import-history/03-CONTEXT.md
@.planning/phases/03-review-queue-and-import-history/03-RESEARCH.md
@.planning/phases/03-review-queue-and-import-history/03-UI-SPEC.md
@.planning/phases/03-review-queue-and-import-history/03-VALIDATION.md
@.planning/phases/03-review-queue-and-import-history/03-02-PLAN.md
@.planning/phases/03-review-queue-and-import-history/03-03-PLAN.md
@src/renderer/features/import/ImportHistoryScreen.tsx
@src/renderer/features/import/ImportBatchDetailScreen.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the dedicated review queue and wire batch re-entry</name>
  <files>src/renderer/App.tsx, src/renderer/mockWalnutApi.ts, src/renderer/features/import/ImportHistoryScreen.tsx, src/renderer/features/import/ImportBatchDetailScreen.tsx, src/renderer/features/import/ReviewQueueScreen.tsx, src/renderer/features/import/ReviewBatchGroup.tsx, src/renderer/features/import/ReviewItemCard.tsx, src/renderer/features/import/ReviewDetailPanel.tsx, src/renderer/features/import/ReviewBulkActionBar.tsx, tests/unit/review-queue.test.tsx</files>
  <read_first>.planning/phases/03-review-queue-and-import-history/03-UI-SPEC.md, src/renderer/App.tsx, src/renderer/mockWalnutApi.ts, src/renderer/features/import/ImportHistoryScreen.tsx, src/renderer/features/import/ImportBatchDetailScreen.tsx, src/shared/contracts/import.ts, tests/unit/import-history.test.tsx</read_first>
  <behavior>
    - Test 1: history and batch detail reopen an unresolved batch directly into the batch-scoped review queue per D-08 and REVW-01.
    - Test 2: the review queue groups unresolved items by batch, orders blocking items before warning items, and keeps source-file context visible per D-02 and the UI contract.
    - Test 3: the queue exposes only the approved single-item and bulk actions and renders amount plus running balance as protected read-only fields per D-14 and D-15.
  </behavior>
  <action>Build a dedicated `ReviewQueueScreen` and supporting components that implement the approved UI contract exactly: unresolved items only per D-04, grouped by batch first per D-02, one active detail panel on smaller layouts, split-view support on wide desktop layouts, and a visible return path back to import history. Render blocking items before warnings, expose the exact single-item and bulk actions from D-03 and D-15, and present protected amount and running-balance fields as muted read-only values with helper copy explaining why they are locked per D-14. Wire `ImportHistoryScreen` and `ImportBatchDetailScreen` reopen actions into the queue so review starts in the chosen batch group rather than a flat global state per D-08. Mirror the queue API in `src/renderer/mockWalnutApi.ts` for browser-harness development and tests.</action>
  <acceptance_criteria>`ReviewQueueScreen.tsx` exists and renders the empty state `No unresolved review items`; queue items display severity, source context, allowed actions, and protected amount/running-balance fields; history and batch detail both open the queue into the correct batch context; bulk actions remain limited to the approved set; `tests/unit/review-queue.test.tsx` covers grouping, protected-field editing rules, and batch-scoped re-entry.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/review-queue.test.tsx</automated>
  </verify>
  <done>The product now has a real dedicated review queue that users can revisit later and navigate with the right batch context and guardrails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire refetch-after-mutation, restore UX, and end-to-end queue coverage</name>
  <files>src/renderer/App.tsx, src/renderer/mockWalnutApi.ts, src/renderer/features/import/ReviewQueueScreen.tsx, src/renderer/features/import/ReviewRestoreBanner.tsx, tests/unit/review-queue.test.tsx, tests/e2e/review-queue.spec.ts</files>
  <read_first>.planning/phases/03-review-queue-and-import-history/03-RESEARCH.md, .planning/phases/03-review-queue-and-import-history/03-UI-SPEC.md, src/renderer/features/import/ReviewQueueScreen.tsx, src/renderer/mockWalnutApi.ts, tests/unit/review-queue.test.tsx</read_first>
  <behavior>
    - Test 1: after resolve or restore actions, the queue, batch detail, and history refetch authoritative state so unresolved counts and status badges update in the same cycle per D-12.
    - Test 2: destructive actions surface a visible persisted restore affordance, not toast-only recovery, per D-17.
    - Test 3: users can complete partial review work, leave, and return later with consistent counts and restore behavior across sessions.
  </behavior>
  <action>Wire the renderer to use refetch-after-mutation over IPC using transition-friendly updates instead of background subscriptions, matching research guidance. Add a persistent restore banner or inline restore affordance for destructive actions in `ReviewRestoreBanner.tsx`, and ensure restore updates queue, batch detail, and history in the same refresh cycle. Cover keyboard movement, partial-progress across sessions, restore flows, and live count or status updates in unit and Playwright tests. Keep all review work within the existing shell and preserve the explicit return path to import history.</action>
  <acceptance_criteria>`ReviewRestoreBanner.tsx` exists and provides visible restore UI after destructive actions; queue, batch detail, and history all reflect updated unresolved counts after resolve and restore; `tests/unit/review-queue.test.tsx` covers refetch-after-mutation and restore UX; `tests/e2e/review-queue.spec.ts` covers reopen, partial resolution across sessions, live status or count refresh, and undo or soft-restore.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/review-queue.test.tsx tests/unit/import/review-mutations.test.ts && cmd /c npx playwright test tests/e2e/review-queue.spec.ts</automated>
  </verify>
  <done>The review queue now feels complete and trustworthy because every mutation updates visible counts immediately and destructive actions can be restored later.</done>
</task>

</tasks>

<verification>
Run the review-queue renderer suite and the Playwright review-queue flow. Confirm unresolved batches reopen correctly, partial progress survives across sessions, destructive actions are restorable, and history or detail counts update from authoritative SQLite state after each mutation.
</verification>

<success_criteria>
Users can leave a batch with unresolved items, come back later through import history, resolve those items in a dedicated queue, and see counts and statuses stay trustworthy throughout the workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-queue-and-import-history/03-04-SUMMARY.md`
</output>
