---
phase: 03-review-queue-and-import-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/contracts/app-state.ts
  - src/shared/contracts/import.ts
  - src/preload/index.ts
  - src/main/persistence/schema.ts
  - src/main/persistence/db.ts
  - src/main/import/import-coordinator.ts
  - src/main/ipc/import.ts
  - tests/unit/import/review-routing.test.ts
  - tests/unit/import/review-gating.test.ts
autonomous: true
requirements:
  - IMPT-05
  - IMPT-06
must_haves:
  truths:
    - "Ambiguous duplicate and parser cases are persisted as review items instead of being guessed or discarded silently."
    - "Blocking review items stop import completion, while warning-level review items allow accepted transactions to remain available."
    - "Every import attempt is durably recorded with a status that can later drive history and review re-entry."
  artifacts:
    - path: "src/shared/contracts/import.ts"
      provides: "Typed review-item, import-attempt, severity, and gating contracts shared across main, preload, and renderer"
      contains: "ReviewItemSeverity"
    - path: "src/main/persistence/db.ts"
      provides: "SQLite transactions for import attempts, review-item creation, and severity-aware batch persistence"
      contains: "persistImportAttempt"
    - path: "tests/unit/import/review-gating.test.ts"
      provides: "Regression coverage for blocking versus warning review outcomes"
      contains: "needs-review"
  key_links:
    - from: "src/main/import/import-coordinator.ts"
      to: "src/main/persistence/db.ts"
      via: "severity-aware batch commit and review-item persistence"
      pattern: "persistImportAttempt|persistReviewItems"
    - from: "src/main/ipc/import.ts"
      to: "src/preload/index.ts"
      via: "typed IPC bridge for history-ready import attempt and review queue data"
      pattern: "import:list-history|import:get-review-queue"
---

<objective>
Create the Phase 3 backend foundation: durable review-item contracts, attempt-level persistence, and severity-aware import gating that preserves trust when the parser is uncertain.

Purpose: Make ambiguity part of the domain model instead of a transient UI condition so later history and queue screens can rely on SQLite as the source of truth.
Output: Shared Phase 3 contracts, persisted import attempts and review items, and coordinator logic that blocks only critical issues while preserving accepted transactions and review state.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-review-queue-and-import-history/03-CONTEXT.md
@.planning/phases/03-review-queue-and-import-history/03-RESEARCH.md
@.planning/phases/03-review-queue-and-import-history/03-UI-SPEC.md
@.planning/phases/03-review-queue-and-import-history/03-VALIDATION.md
@.planning/phases/02-statement-import-pipeline/02-01-SUMMARY.md
@.planning/phases/02-statement-import-pipeline/02-02-SUMMARY.md
@src/shared/contracts/import.ts
@src/main/import/import-coordinator.ts
@src/main/persistence/db.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Phase 2 import contracts into durable Phase 3 review and attempt models</name>
  <files>src/shared/contracts/app-state.ts, src/shared/contracts/import.ts, src/preload/index.ts, src/main/ipc/import.ts, tests/unit/import/review-routing.test.ts, tests/unit/import/review-gating.test.ts</files>
  <read_first>.planning/phases/03-review-queue-and-import-history/03-CONTEXT.md, .planning/phases/03-review-queue-and-import-history/03-RESEARCH.md, .planning/phases/03-review-queue-and-import-history/03-UI-SPEC.md, src/shared/contracts/app-state.ts, src/shared/contracts/import.ts, src/preload/index.ts, src/main/ipc/import.ts</read_first>
  <behavior>
    - Test 1: ambiguous duplicate candidates, parser uncertainty rows, deferred worksheet decisions, balance continuity warnings, and unsupported skipped rows are mapped into typed review items per D-01.
    - Test 2: review items carry `blocking` or `warning` severity so import completion logic can distinguish stop-work issues from queue-later issues per D-09 and IMPT-06.
    - Test 3: import attempts expose statuses `imported`, `needs-review`, `rejected`, and `failed` with summary counters needed by later history views per D-05, D-06, and D-12.
  </behavior>
  <action>Expand `src/shared/contracts/import.ts` to define the Phase 3 contract surface: typed `ReviewItemReasonCode`, `ReviewItemSeverity`, `ReviewItemState`, `ReviewItemSnapshot`, `ImportAttemptStatus`, `ImportAttemptSummary`, `ImportBatchDetail`, queue query inputs, and resolution-ready identifiers. Keep the model batch-first per D-02 and unresolved-only per D-04. Encode all unresolved types from D-01 exactly once in the reason-code union. Update `src/shared/contracts/app-state.ts` so the canonical `WalnutApi` interface owns `listImportHistory`, `getImportBatchDetail`, and `getReviewQueue`, and then mirror those placeholders through preload and IPC. Create the failing routing and gating tests first so Wave 1 defines how ambiguous parser/duplicate outcomes must be converted into durable review items instead of silent acceptance, satisfying IMPT-05 and the gating half of IMPT-06.</action>
  <acceptance_criteria>`src/shared/contracts/import.ts` contains `ReviewItemReasonCode`, `ImportAttemptSummary`, `ReviewItemSeverity`, and `ReviewItemState`; the reason-code union includes duplicate candidates, parser uncertainty, deferred worksheet, balance continuity warning, and unsupported skipped rows; `src/shared/contracts/app-state.ts` and `src/preload/index.ts` expose `listImportHistory`, `getImportBatchDetail`, and `getReviewQueue`; the new unit tests fail before implementation and describe warning versus blocking review behavior explicitly.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/import/review-routing.test.ts tests/unit/import/review-gating.test.ts</automated>
  </verify>
  <done>Phase 3 has stable shared contracts and failing tests that lock in review routing and mixed gating behavior before persistence and coordinator code is written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist import attempts and review items with severity-aware coordinator behavior</name>
  <files>src/main/persistence/schema.ts, src/main/persistence/db.ts, src/main/import/import-coordinator.ts, src/main/ipc/import.ts, tests/unit/import/review-routing.test.ts, tests/unit/import/review-gating.test.ts</files>
  <read_first>.planning/phases/03-review-queue-and-import-history/03-CONTEXT.md, .planning/phases/03-review-queue-and-import-history/03-RESEARCH.md, src/main/persistence/schema.ts, src/main/persistence/db.ts, src/main/import/import-coordinator.ts, tests/unit/import/review-routing.test.ts, tests/unit/import/review-gating.test.ts</read_first>
  <behavior>
    - Test 1: batches with warning-only review items persist accepted transactions, create durable review items, and finish with `needs-review`.
    - Test 2: batches with blocking review items persist the attempt and review items but do not finalize accepted transactions until the blocker is resolved.
    - Test 3: every attempt, including rejected or failed imports, is queryable later with counts and timestamps instead of disappearing after the import screen closes.
  </behavior>
  <action>Add SQLite tables and indexes for `import_attempts`, `review_items`, and any supporting audit-ready resolution metadata inside `src/main/persistence/schema.ts` and `src/main/persistence/db.ts`, using the batch-first model recommended in research. Extend repository transactions so one import attempt can persist accepted rows, per-file outcomes, unresolved review items, and summary counters atomically. Update `ImportCoordinator` to classify ambiguous duplicate or parser outputs into review items per D-01 and IMPT-05, apply severity-aware gating per IMPT-06 and D-09, and preserve accepted transactions for warning-only batches per D-10. Use refetchable SQLite-backed state, not in-memory unresolved lists, per D-11 and research guidance. Register the new history/detail/queue read IPC handlers in `src/main/ipc/import.ts`, but keep mutation work for the dedicated review plan. Do not allow amount or running-balance edits in this wave; this plan only establishes protected source-of-truth persistence per D-14.</action>
  <acceptance_criteria>`src/main/persistence/schema.ts` contains `import_attempts` and `review_items`; `src/main/persistence/db.ts` contains repository methods for persisting attempts and reading unresolved review state; `ImportCoordinator` can return `needs-review`, `rejected`, and `failed` attempt outcomes in addition to imported success; warning-only batches still surface accepted transactions while blocking batches do not finalize them; the routing and gating tests pass.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/import/review-routing.test.ts tests/unit/import/review-gating.test.ts</automated>
  </verify>
  <done>Ambiguous imports are now durably modeled in SQLite, and import completion behaves exactly as the Phase 3 trust posture requires.</done>
</task>

</tasks>

<verification>
Run the new review-routing and review-gating unit suites. Confirm the coordinator records ambiguous cases as review items, persists every import attempt, and distinguishes warning-only batches from blocking failures without relying on renderer state.
</verification>

<success_criteria>
Phase 3 has a reliable domain foundation: uncertainty is persisted, mixed gating works, and later history and review surfaces can query a durable SQLite source of truth.
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-queue-and-import-history/03-01-SUMMARY.md`
</output>
