---
phase: 03-review-queue-and-import-history
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/shared/contracts/app-state.ts
  - src/shared/contracts/import.ts
  - src/preload/index.ts
  - src/main/persistence/db.ts
  - src/main/ipc/import.ts
  - src/renderer/App.tsx
  - src/renderer/mockWalnutApi.ts
  - src/renderer/features/import/ImportWorkspace.tsx
  - src/renderer/features/import/ImportHistoryScreen.tsx
  - src/renderer/features/import/ImportBatchDetailScreen.tsx
  - src/renderer/features/import/ImportStatusBadge.tsx
  - tests/unit/import/history-repository.test.ts
  - tests/unit/import-history.test.tsx
autonomous: true
requirements:
  - IMPT-07
must_haves:
  truths:
    - "Users can see every import attempt, including imported, needs-review, rejected, and failed outcomes."
    - "Import history rows show the required counts and status badges without renderer-side recomputation drift."
    - "Batch detail works as a receipt view with summary, per-file outcomes, and transaction drill-down tied back to one attempt."
  artifacts:
    - path: "src/renderer/features/import/ImportHistoryScreen.tsx"
      provides: "Operational import-history ledger with newest-first attempt rows and batch re-entry actions"
      contains: "No imports yet"
    - path: "src/renderer/features/import/ImportBatchDetailScreen.tsx"
      provides: "Batch receipt view with summary strip, per-file outcomes, and transaction drill-down"
      contains: "Review unresolved items"
    - path: "tests/unit/import/history-repository.test.ts"
      provides: "Repository-level regression coverage for all-attempt history queries and live counters"
      contains: "failed"
  key_links:
    - from: "src/main/persistence/db.ts"
      to: "src/renderer/features/import/ImportHistoryScreen.tsx"
      via: "history summary and detail IPC queries"
      pattern: "listImportHistory|getImportBatchDetail"
    - from: "src/renderer/App.tsx"
      to: "src/renderer/features/import/ImportBatchDetailScreen.tsx"
      via: "shell navigation from import workspace into history and batch detail"
      pattern: "history|batch-detail"
---

<objective>
Expose the persisted import-attempt ledger through import history and batch detail screens that make every batch outcome inspectable and reopenable.

Purpose: Turn the Phase 3 persistence foundation into a trustworthy operational receipt so owners can understand what happened even after the import session ends.
Output: Indexed history/detail queries, history and batch-detail renderer screens, and tests covering all-attempt visibility plus count integrity.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-review-queue-and-import-history/03-CONTEXT.md
@.planning/phases/03-review-queue-and-import-history/03-RESEARCH.md
@.planning/phases/03-review-queue-and-import-history/03-UI-SPEC.md
@.planning/phases/03-review-queue-and-import-history/03-VALIDATION.md
@.planning/phases/03-review-queue-and-import-history/03-01-PLAN.md
@src/shared/contracts/import.ts
@src/main/persistence/db.ts
@src/renderer/features/import/ImportWorkspace.tsx
@src/renderer/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build repository and IPC history/detail queries for all import attempts</name>
  <files>src/shared/contracts/app-state.ts, src/shared/contracts/import.ts, src/preload/index.ts, src/main/persistence/db.ts, src/main/ipc/import.ts, tests/unit/import/history-repository.test.ts</files>
  <read_first>.planning/phases/03-review-queue-and-import-history/03-RESEARCH.md, .planning/phases/03-review-queue-and-import-history/03-UI-SPEC.md, src/shared/contracts/app-state.ts, src/shared/contracts/import.ts, src/main/persistence/db.ts, src/main/ipc/import.ts, tests/unit/import/review-routing.test.ts, tests/unit/import/review-gating.test.ts</read_first>
  <behavior>
    - Test 1: history queries return imported, needs-review, rejected, and failed attempts newest first with date/time, account, file count, accepted count, blocked duplicates, unresolved review count, and status badge fields per D-05 and D-06.
    - Test 2: batch detail returns a summary strip, per-file outcomes, and read-only transaction drill-down per D-07.
    - Test 3: history counters stay authoritative after review-state changes because queries read repository aggregates instead of renderer recalculation per D-12.
  </behavior>
  <action>Implement repository methods and IPC handlers for `listImportHistory` and `getImportBatchDetail`, using SQLite aggregate queries and indexes rather than renderer `reduce()` logic. Return all attempts, not just successful imports, satisfying IMPT-07 and D-06 exactly. Make the summary payload expose the exact row fields from D-05 and the batch detail shape needed by the UI contract: summary strip, grouped file outcomes, and read-only transaction drill-down per D-07. Reuse the attempt and review-item tables from 03-01, ensure unresolved counts derive from unresolved review items so later refetch-after-mutation work updates live without extra cache machinery, and update `src/shared/contracts/app-state.ts` so the shared API surface type-checks cleanly.</action>
  <acceptance_criteria>`src/main/persistence/db.ts` exposes `listImportHistory` and `getImportBatchDetail`; `src/shared/contracts/app-state.ts` includes both methods on `WalnutApi`; results include status plus accepted, blocked-duplicate, unresolved, and error counts; `tests/unit/import/history-repository.test.ts` covers imported, needs-review, rejected, and failed attempts as well as transaction drill-down and counter integrity.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/import/history-repository.test.ts</automated>
  </verify>
  <done>The backend can now answer the exact import-history and batch-detail questions the Phase 3 UI needs, with all-attempt coverage and stable counters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add import history and batch detail screens inside the existing shell</name>
  <files>src/renderer/App.tsx, src/renderer/mockWalnutApi.ts, src/renderer/features/import/ImportWorkspace.tsx, src/renderer/features/import/ImportHistoryScreen.tsx, src/renderer/features/import/ImportBatchDetailScreen.tsx, src/renderer/features/import/ImportStatusBadge.tsx, tests/unit/import-history.test.tsx</files>
  <read_first>.planning/phases/03-review-queue-and-import-history/03-UI-SPEC.md, src/renderer/App.tsx, src/renderer/features/import/ImportWorkspace.tsx, src/renderer/mockWalnutApi.ts, src/shared/contracts/import.ts, tests/unit/import-workspace.test.tsx</read_first>
  <behavior>
    - Test 1: import history renders newest-first rows with the canonical statuses `Imported`, `Needs review`, `Rejected`, and `Failed`, plus the required row counts and the empty-state copy from the UI spec.
    - Test 2: clicking a history row opens batch detail with summary strip, per-file outcomes, and read-only transaction drill-down.
    - Test 3: batches with unresolved items expose `Open review queue` in history and `Review unresolved items` in batch detail, preserving batch context for the next plan.
  </behavior>
  <action>Extend the current import area inside `App.tsx` so the owner can move between the existing `ImportWorkspace`, a new `ImportHistoryScreen`, and a new `ImportBatchDetailScreen` without leaving the shell. Implement the UI contract exactly: history is an operational ledger, newest first, showing all attempts per D-05 and D-06; batch detail is a three-section receipt view per D-07; unresolved batches reserve a direct reopen action per D-08. Use the existing token system and Phase 2 import card language, not a new component library. Mirror the new API surface in `src/renderer/mockWalnutApi.ts` so browser-harness tests can render imported, needs-review, rejected, and failed attempts. Keep transaction drill-down read-only in this plan and defer review mutations to 03-03.</action>
  <acceptance_criteria>`ImportHistoryScreen.tsx` and `ImportBatchDetailScreen.tsx` exist; history renders the empty state `No imports yet`; history rows show date/time, account, file count, accepted count, blocked duplicate count, unresolved review count, and status badge; batch detail renders `Review unresolved items` only when unresolved items exist; `tests/unit/import-history.test.tsx` covers history rows, batch detail navigation, and unresolved-batch reopen actions.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/import-history.test.tsx tests/unit/import/history-repository.test.ts</automated>
  </verify>
  <done>Import history and batch detail are now usable product surfaces, not just repository plumbing, and they preserve the batch context needed for review re-entry.</done>
</task>

</tasks>

<verification>
Run the history repository and renderer tests. Confirm every import attempt is visible, history rows expose the required operational counts, and batch detail functions as a receipt view with direct review re-entry for unresolved batches.
</verification>

<success_criteria>
Users can revisit import history later, inspect any batch outcome, and understand batch-level counts and errors without reopening the original import session.
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-queue-and-import-history/03-02-SUMMARY.md`
</output>
