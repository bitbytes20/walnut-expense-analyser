---
phase: 06-dashboard-analytics
plan: 04
type: execute
wave: 4
depends_on:
  - 06-01-PLAN.md
  - 06-02-PLAN.md
  - 06-03-PLAN.md
files_modified:
  - tests/unit/dashboard/dashboard-repository.test.ts
  - tests/unit/dashboard/date-range-prefs.test.ts
  - tests/unit/dashboard/recurring-detection.test.ts
  - tests/unit/dashboard/drilldown-query.test.ts
  - tests/unit/dashboard-screen.test.tsx
  - tests/e2e/dashboard-analytics.spec.ts
  - .planning/phases/06-dashboard-analytics/06-VERIFICATION.md
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "Phase 6 is not complete until dashboard math, compact-mode layout, comparisons, and drill-down interactions are covered by both unit and end-to-end tests."
    - "Dashboard performance and correctness matter more than animation density or optional visual flourish."
    - "The dashboard must remain trustworthy in both empty and data-rich states, across light and dark themes."
---

<objective>
Close Phase 6 with full regression coverage, end-to-end dashboard flows, and a formal verification handoff.

Purpose: Prove the dashboard analytics surface is correct, fast-feeling, and stable enough for downstream audit and settings phases to build on.
Output: Complete unit and e2e coverage for dashboard behavior plus a verification report mapped back to DASH-01 through DASH-04.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dashboard-analytics/06-VALIDATION.md
@.planning/phases/06-dashboard-analytics/06-UI-SPEC.md
@tests/unit/dashboard/dashboard-repository.test.ts
@tests/unit/dashboard/date-range-prefs.test.ts
@tests/unit/dashboard/recurring-detection.test.ts
@tests/unit/dashboard/drilldown-query.test.ts
@tests/unit/dashboard-screen.test.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fill the Phase 6 unit and end-to-end coverage gaps</name>
  <files>tests/unit/dashboard/dashboard-repository.test.ts, tests/unit/dashboard/date-range-prefs.test.ts, tests/unit/dashboard/recurring-detection.test.ts, tests/unit/dashboard/drilldown-query.test.ts, tests/unit/dashboard-screen.test.tsx, tests/e2e/dashboard-analytics.spec.ts</files>
  <read_first>.planning/phases/06-dashboard-analytics/06-VALIDATION.md, .planning/phases/06-dashboard-analytics/06-UI-SPEC.md, tests/unit/dashboard-screen.test.tsx, tests/unit/dashboard/dashboard-repository.test.ts</read_first>
  <behavior>
    - verify summary math, comparisons, category/merchant rankings, recurring detection, and operational cards
    - verify presets, custom range, compare toggle, compact mode, and widget ordering
    - verify drill-down into the Transactions workspace and recurring-detail flow end-to-end
  </behavior>
  <action>Fill all Wave 0 gaps declared in `06-VALIDATION.md`. Expand repository tests for snapshot math, comparisons, recurring detection, and rankings. Expand renderer tests for compact-mode layout, summary rows, widget-local control behavior, and preview widgets. Add a Playwright spec named `tests/e2e/dashboard-analytics.spec.ts` that imports realistic data, opens the dashboard, switches presets and custom ranges, toggles comparison, clicks a chart or category widget into the ledger, opens a recurring-detail panel, and verifies recent-transactions preview click-through behavior.</action>
  <acceptance_criteria>All planned Phase 6 unit and end-to-end tests exist and pass; the e2e flow covers global range controls, comparison behavior, dashboard drill-down, recurring-detail flow, and dashboard-to-ledger continuity; repository and renderer suites cover DASH-01 through DASH-04.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/dashboard/*.test.ts tests/unit/dashboard-screen.test.tsx && cmd /c npx playwright test tests/e2e/dashboard-analytics.spec.ts</automated>
  </verify>
  <done>Phase 6 dashboard behavior is protected by a full regression surface rather than ad hoc manual checking.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Complete formal verification and execution handoff</name>
  <files>.planning/phases/06-dashboard-analytics/06-VERIFICATION.md</files>
  <read_first>.planning/phases/06-dashboard-analytics/06-VALIDATION.md, .planning/phases/06-dashboard-analytics/06-CONTEXT.md, .planning/phases/06-dashboard-analytics/06-RESEARCH.md, .planning/phases/06-dashboard-analytics/06-UI-SPEC.md</read_first>
  <behavior>
    - record the executed commands and outcomes
    - map validation back to DASH-01 through DASH-04
    - note residual risks or follow-up work clearly without stretching scope
  </behavior>
  <action>Write the Phase 6 verification report after all tests pass. Include the exact final commands, requirement mapping for `DASH-01` through `DASH-04`, notes on comparison behavior, compact-mode responsiveness, recurring-detail flow, and any residual follow-up items that belong in Phase 7, Phase 8, or Release 2 instead of silently extending dashboard scope.</action>
  <acceptance_criteria>`06-VERIFICATION.md` exists, references the executed commands, closes the loop on DASH-01 through DASH-04, and explicitly calls out any remaining residual risks.</acceptance_criteria>
  <verify>
    <automated>cmd /c npm run test:unit && cmd /c npx playwright test tests/e2e/dashboard-analytics.spec.ts</automated>
  </verify>
  <done>Phase 6 ends with a clean verification artifact that downstream audit, diagnostics, and settings phases can trust.</done>
</task>

</tasks>

<verification>
Run the full Phase 6 unit suite slice and the dedicated dashboard Playwright flow. Confirm dashboard math, comparisons, compact-mode layout, recurring-detail behavior, and ledger drill-down all behave as planned.
</verification>

<success_criteria>
Phase 6 is complete only when the dashboard is both implemented and verified: it must deliver trustworthy insight blocks, fast-feeling local interactions, and a polished analytics surface across both themes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-analytics/06-04-SUMMARY.md`
</output>
