---
phase: 06-dashboard-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - src/renderer/App.tsx
  - src/renderer/features/dashboard/DashboardScreen.tsx
  - src/renderer/features/dashboard/DashboardHeader.tsx
  - src/renderer/features/dashboard/SummaryCardRow.tsx
  - src/renderer/features/dashboard/SpendTrendCard.tsx
  - src/renderer/features/dashboard/CategoryBreakdownCard.tsx
  - src/renderer/features/dashboard/TopMerchantsCard.tsx
  - src/renderer/features/dashboard/OperationalSummaryRow.tsx
  - tests/unit/dashboard-screen.test.tsx
  - tests/unit/dashboard/date-range-prefs.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "The dashboard is a premium hybrid workspace with a strong summary row and a dense analytics grid, not a sparse empty-state page."
    - "Compact mode is a first-class layout behavior for smaller desktop windows, not an afterthought."
    - "Global date controls anchor the page while widget-local controls remain subordinate and legible."
  artifacts:
    - path: "src/renderer/features/dashboard/DashboardScreen.tsx"
      provides: "Primary Phase 6 analytics workspace with summary row and dense grid"
      contains: "Dashboard"
    - path: "src/renderer/features/dashboard/SpendTrendCard.tsx"
      provides: "Primary trend visualization with comparison-aware charting"
      contains: "Compare with previous period"
    - path: "tests/unit/dashboard-screen.test.tsx"
      provides: "Renderer coverage for dashboard layout, compact mode, and widget ordering"
      contains: "top merchants"
  key_links:
    - from: "src/renderer/features/dashboard/DashboardHeader.tsx"
      to: "src/preload/index.ts"
      via: "global range selection and preference persistence"
      pattern: "getDashboardPreferences|setDashboardPreferences"
    - from: "src/renderer/features/dashboard/DashboardScreen.tsx"
      to: "src/shared/contracts/dashboard.ts"
      via: "snapshot-driven rendering for cards, charts, and supporting widgets"
      pattern: "summaryCards|operationalCards|recentTransactions"
---

<objective>
Build the primary Dashboard workspace: the premium analytics layout, date controls, summary cards, spend trend, category breakdown, top merchants, and responsive compact-mode behavior.

Purpose: Replace the placeholder dashboard with the first real insight surface that feels fast, premium, and readable at a glance.
Output: A live Dashboard screen with global presets, optional comparison, summary rows, primary widgets, compact-mode responsiveness, and typed renderer coverage.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-analytics/06-CONTEXT.md
@.planning/phases/06-dashboard-analytics/06-RESEARCH.md
@.planning/phases/06-dashboard-analytics/06-UI-SPEC.md
@.planning/phases/06-dashboard-analytics/06-VALIDATION.md
@src/renderer/features/dashboard/EmptyDashboard.tsx
@src/renderer/styles/tokens.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the live dashboard workspace and top analytics band</name>
  <files>src/renderer/App.tsx, src/renderer/features/dashboard/DashboardScreen.tsx, src/renderer/features/dashboard/DashboardHeader.tsx, src/renderer/features/dashboard/SummaryCardRow.tsx, src/renderer/features/dashboard/SpendTrendCard.tsx, src/renderer/features/dashboard/CategoryBreakdownCard.tsx, src/renderer/features/dashboard/TopMerchantsCard.tsx, tests/unit/dashboard-screen.test.tsx</files>
  <read_first>.planning/phases/06-dashboard-analytics/06-UI-SPEC.md, src/renderer/features/dashboard/EmptyDashboard.tsx, src/renderer/App.tsx, src/renderer/styles/tokens.css</read_first>
  <behavior>
    - Test 1: the dashboard renders a strong top summary row followed by the trend chart, category breakdown, and top merchants in the required order.
    - Test 2: the dashboard reads from the live snapshot contract rather than the old empty-state-only layout when data exists.
    - Test 3: the dashboard compact mode reflows the analytics grid and preserves summary-first hierarchy on smaller desktop widths.
  </behavior>
  <action>Create `DashboardScreen.tsx` and the supporting widget components to render the Phase 6 analytics surface in the existing shell. Wire the app so the dashboard shows live analytics when data exists and still retains a clean empty state when there is no dashboard data yet. Use the current Walnut palette and tokens, keep the top scan order locked from the UI spec, and ensure compact desktop widths are handled intentionally rather than by passive overflow.</action>
  <acceptance_criteria>`DashboardScreen.tsx` exists and renders the required top summary and analytics widgets; the dashboard replaces the old placeholder when data exists; renderer tests cover widget order and compact-mode layout behavior.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/dashboard-screen.test.tsx</automated>
  </verify>
  <done>The product now has a real dashboard surface with the correct hierarchy, layout, and desktop responsiveness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire date controls, comparison behavior, and operational summary cards</name>
  <files>src/renderer/features/dashboard/DashboardHeader.tsx, src/renderer/features/dashboard/SummaryCardRow.tsx, src/renderer/features/dashboard/OperationalSummaryRow.tsx, src/renderer/features/dashboard/SpendTrendCard.tsx, tests/unit/dashboard-screen.test.tsx, tests/unit/dashboard/date-range-prefs.test.ts</files>
  <read_first>.planning/phases/06-dashboard-analytics/06-CONTEXT.md, .planning/phases/06-dashboard-analytics/06-UI-SPEC.md, src/shared/contracts/dashboard.ts</read_first>
  <behavior>
    - Test 1: quick presets, custom range, compare toggle, and remembered last selection all work through the dashboard header.
    - Test 2: key summary cards and trend charts show previous-period comparisons when enabled.
    - Test 3: operational summary cards for transfers, refunds, ATM withdrawals, and credit-card payments appear together and remain visually secondary to the main summary row.
  </behavior>
  <action>Build the dashboard header controls for presets, custom range, and compare toggle, and wire them through the preference and snapshot APIs. Render comparison-aware key summary cards and the supporting operational summary row. Keep widget-local controls subordinate to the global header and ensure the comparison model stays visible and literal for both preset and custom ranges.</action>
  <acceptance_criteria>The dashboard header supports presets, custom ranges, and compare toggles; summary cards and charts can show previous-period comparisons; the operational cards appear together and remain secondary in hierarchy; tests cover range and comparison behavior.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/dashboard-screen.test.tsx tests/unit/dashboard/date-range-prefs.test.ts</automated>
  </verify>
  <done>Phase 6 now has a usable global analytics control surface with visible comparison behavior and complete top-of-page summary context.</done>
</task>

</tasks>

<verification>
Run the dashboard screen and date-range preference suites. Confirm widget ordering, compact-mode behavior, global controls, comparison rendering, and the operational summary row all behave as planned.
</verification>

<success_criteria>
Phase 6 now exposes a premium dashboard shell that immediately communicates household inflow, outflow, change over time, and major spend areas.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-analytics/06-02-SUMMARY.md`
</output>
