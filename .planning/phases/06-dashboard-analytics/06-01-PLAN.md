---
phase: 06-dashboard-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/contracts/dashboard.ts
  - src/shared/contracts/app-state.ts
  - src/preload/index.ts
  - src/main/persistence/schema.ts
  - src/main/persistence/db.ts
  - src/main/ipc/dashboard.ts
  - tests/unit/dashboard/dashboard-repository.test.ts
  - tests/unit/dashboard/date-range-prefs.test.ts
  - tests/unit/dashboard/recurring-detection.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "Dashboard analytics are repository-owned aggregate queries, not renderer-side reductions over the full ledger."
    - "Dashboard math is type-driven: refunds count as income, transfers are excluded from income-vs-expense, and spending widgets include ATM withdrawals and credit-card payments."
    - "Global range and compare preferences persist locally so the dashboard can reopen in the user's last working state."
  artifacts:
    - path: "src/shared/contracts/dashboard.ts"
      provides: "Typed dashboard snapshot, widget contracts, recurring-detail payloads, and drill-down query seeds"
      contains: "DashboardSnapshotSchema"
    - path: "src/main/persistence/db.ts"
      provides: "Repository-backed snapshot aggregation, period comparison math, and recurring detection"
      contains: "getDashboardSnapshot"
    - path: "tests/unit/dashboard/dashboard-repository.test.ts"
      provides: "Regression coverage for dashboard summary math, chart buckets, and ranking widgets"
      contains: "returns summary cards for the selected range"
  key_links:
    - from: "src/main/ipc/dashboard.ts"
      to: "src/preload/index.ts"
      via: "typed IPC bridge for dashboard snapshot and recurring detail requests"
      pattern: "dashboard:getSnapshot|dashboard:getRecurringDetail"
    - from: "src/main/persistence/db.ts"
      to: "src/shared/contracts/dashboard.ts"
      via: "contract-shaped aggregate payloads and compare-period metadata"
      pattern: "summaryCards|spendTrend|categoryBreakdown"
---

<objective>
Create the Phase 6 backend foundation: typed dashboard contracts, repository-owned aggregate queries, recurring detection, and persistent global date-range preferences.

Purpose: Ensure the dashboard is built on fast, correct local analytics instead of renderer-side derivation or ad hoc widget queries.
Output: Shared dashboard contracts, SQLite-backed snapshot queries, compare-period logic, recurring-detail payloads, and persisted dashboard preferences.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-analytics/06-CONTEXT.md
@.planning/phases/06-dashboard-analytics/06-RESEARCH.md
@.planning/phases/06-dashboard-analytics/06-UI-SPEC.md
@.planning/phases/06-dashboard-analytics/06-VALIDATION.md
@src/shared/contracts/app-state.ts
@src/main/persistence/db.ts
@src/shared/contracts/transactions.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define dashboard contracts and repository-backed snapshot queries</name>
  <files>src/shared/contracts/dashboard.ts, src/preload/index.ts, src/main/persistence/schema.ts, src/main/persistence/db.ts, src/main/ipc/dashboard.ts, tests/unit/dashboard/dashboard-repository.test.ts, tests/unit/dashboard/recurring-detection.test.ts</files>
  <read_first>.planning/phases/06-dashboard-analytics/06-CONTEXT.md, .planning/phases/06-dashboard-analytics/06-RESEARCH.md, .planning/phases/06-dashboard-analytics/06-UI-SPEC.md, src/main/persistence/db.ts, src/shared/contracts/transactions.ts</read_first>
  <behavior>
    - Test 1: dashboard snapshot returns summary cards, trend series, category breakdown, top merchants, largest transactions, recent transaction preview, recurring items, and operational summary cards for the selected range.
    - Test 2: income-vs-expense and supporting cards follow the locked type-driven math rules, including refunds as income and transfers excluded.
    - Test 3: recurring detection includes both recurring debits and recurring credits and exposes a recurring-detail payload shape for the renderer.
  </behavior>
  <action>Create `src/shared/contracts/dashboard.ts` as the new Phase 6 contract module. Define typed schemas for the dashboard query input, global range presets, compare toggle, summary cards, trend series points, category and merchant widgets, recent-transaction preview rows, largest-transaction rows, operational cards, recurring summaries, and recurring-detail payloads. Add the repository method `getDashboardSnapshot` plus a companion `getRecurringDetail` in `src/main/persistence/db.ts`, backed by targeted aggregate queries and indexes in `src/main/persistence/schema.ts`. Register typed IPC handlers in `src/main/ipc/dashboard.ts` and expose them through preload so the renderer can fetch one coherent snapshot rather than per-widget payloads.</action>
  <acceptance_criteria>`src/shared/contracts/dashboard.ts` exists and contains `DashboardSnapshotSchema` and `RecurringDetailSchema`; `src/main/persistence/db.ts` contains `getDashboardSnapshot`; repository tests prove the locked dashboard math and recurring detection rules; typed dashboard IPC methods are exposed through preload.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/dashboard/dashboard-repository.test.ts tests/unit/dashboard/recurring-detection.test.ts</automated>
  </verify>
  <done>Phase 6 now has a fast, contract-driven analytics backend capable of powering the premium dashboard surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist dashboard date-range and comparison preferences</name>
  <files>src/shared/contracts/dashboard.ts, src/shared/contracts/app-state.ts, src/preload/index.ts, src/main/persistence/db.ts, src/main/ipc/dashboard.ts, tests/unit/dashboard/date-range-prefs.test.ts</files>
  <read_first>.planning/phases/06-dashboard-analytics/06-RESEARCH.md, src/shared/contracts/app-state.ts, src/main/persistence/db.ts, src/preload/index.ts</read_first>
  <behavior>
    - Test 1: global presets, custom ranges, and compare-toggle state persist locally and can be restored on dashboard reopen.
    - Test 2: custom-range compare behavior is represented explicitly rather than guessed.
    - Test 3: persisted preferences remain separate from widget-local controls.
  </behavior>
  <action>Extend the local settings/app-state persistence model so dashboard preferences include the remembered global date range, custom range bounds, and compare-toggle state. Add typed getter/setter repository and IPC methods for this dashboard preference payload and write tests that lock the preference model before renderer work begins.</action>
  <acceptance_criteria>Dashboard preference contracts exist; app-state persistence can read and write the remembered dashboard range; tests prove preset/custom/compare values restore correctly and stay independent from widget-local controls.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/dashboard/date-range-prefs.test.ts</automated>
  </verify>
  <done>The dashboard can reopen in the household's last global analytical context without leaking widget-local overrides into persistent settings.</done>
</task>

</tasks>

<verification>
Run the dashboard repository, recurring-detection, and date-range preference suites. Confirm snapshot math follows the locked product rules and preferences restore correctly for global dashboard state.
</verification>

<success_criteria>
Phase 6 has a trustworthy analytics foundation: one typed dashboard snapshot API, correct money math, recurring detection, and persisted global range preferences.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-analytics/06-01-SUMMARY.md`
</output>
