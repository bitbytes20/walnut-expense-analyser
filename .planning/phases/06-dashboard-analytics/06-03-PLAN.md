---
phase: 06-dashboard-analytics
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01-PLAN.md
  - 06-02-PLAN.md
files_modified:
  - src/renderer/features/dashboard/DashboardScreen.tsx
  - src/renderer/features/dashboard/LargestTransactionsCard.tsx
  - src/renderer/features/dashboard/RecentTransactionsPreview.tsx
  - src/renderer/features/dashboard/RecurringActivityCard.tsx
  - src/renderer/features/dashboard/RecurringDetailPanel.tsx
  - src/renderer/features/dashboard/dashboard-drilldown.ts
  - src/renderer/features/transactions/TransactionsScreen.tsx
  - tests/unit/dashboard-screen.test.tsx
  - tests/unit/dashboard/drilldown-query.test.ts
  - tests/unit/dashboard/recurring-detection.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-03
must_haves:
  truths:
    - "Dashboard interactivity reuses the trusted Transactions workspace instead of creating a second ad hoc analysis surface."
    - "Recent transactions remain a preview only, while recurring items get their own detail panel before deeper navigation."
    - "Drill-down behavior is widget-specific but always preserves user orientation and filter intent."
  artifacts:
    - path: "src/renderer/features/dashboard/dashboard-drilldown.ts"
      provides: "Shared mapping from dashboard widget interactions into TransactionLedgerQuery payloads"
      contains: "toCategoryDrilldownQuery"
    - path: "src/renderer/features/dashboard/RecurringDetailPanel.tsx"
      provides: "Recurring-item detail view prior to ledger navigation"
      contains: "Recurring activity"
    - path: "tests/unit/dashboard/drilldown-query.test.ts"
      provides: "Coverage for drill-down query shaping from widgets into ledger filters"
      contains: "maps chart bar click into ledger filters"
  key_links:
    - from: "src/renderer/features/dashboard/dashboard-drilldown.ts"
      to: "src/shared/contracts/transactions.ts"
      via: "reuse of TransactionLedgerQuery for dashboard click-through flows"
      pattern: "TransactionLedgerQuery"
    - from: "src/renderer/features/dashboard/RecurringActivityCard.tsx"
      to: "src/renderer/features/dashboard/RecurringDetailPanel.tsx"
      via: "open detail-first recurring flow"
      pattern: "selectedRecurringId"
---

<objective>
Complete the interactive dashboard behaviors: recent transaction preview, largest transactions, recurring detail flow, and widget-driven drill-down into the ledger.

Purpose: Make the dashboard analytically useful without bloating it into a second transaction workspace.
Output: Interactive dashboard widgets with predictable click-through behavior, recurring-detail panel flow, and typed ledger drill-down mapping.
</objective>

<execution_context>
@C:/Users/BitBytes/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/BitBytes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-analytics/06-CONTEXT.md
@.planning/phases/06-dashboard-analytics/06-RESEARCH.md
@.planning/phases/06-dashboard-analytics/06-UI-SPEC.md
@.planning/phases/06-dashboard-analytics/06-VALIDATION.md
@src/shared/contracts/dashboard.ts
@src/shared/contracts/transactions.ts
@src/renderer/features/transactions/TransactionsScreen.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement dashboard drill-down and recurring-detail behaviors</name>
  <files>src/renderer/features/dashboard/DashboardScreen.tsx, src/renderer/features/dashboard/RecurringActivityCard.tsx, src/renderer/features/dashboard/RecurringDetailPanel.tsx, src/renderer/features/dashboard/dashboard-drilldown.ts, tests/unit/dashboard/drilldown-query.test.ts, tests/unit/dashboard/recurring-detection.test.ts, tests/unit/dashboard-screen.test.tsx</files>
  <read_first>.planning/phases/06-dashboard-analytics/06-CONTEXT.md, .planning/phases/06-dashboard-analytics/06-UI-SPEC.md, src/shared/contracts/dashboard.ts, src/shared/contracts/transactions.ts</read_first>
  <behavior>
    - Test 1: clicking chart segments or bars maps to ledger filters and navigates into the Transactions workspace with the correct query state.
    - Test 2: recurring items open a recurring-detail panel first instead of immediately leaving the dashboard.
    - Test 3: widget-specific drill-down behavior stays predictable and preserves the analytical context.
  </behavior>
  <action>Create a small shared drill-down mapping layer that turns dashboard widget interactions into `TransactionLedgerQuery` payloads. Use it for chart segments, category and merchant interactions, and largest-transaction click-through flows. Build a recurring-detail panel that opens from recurring items first, using the recurring-detail payload from the backend, with onward navigation into the ledger only from that context.</action>
  <acceptance_criteria>Drill-down mapping is centralized; charts and key widgets can open the ledger with scoped filters; recurring items open a detail panel first; tests cover drill-down shaping and recurring-panel behavior.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/dashboard/drilldown-query.test.ts tests/unit/dashboard/recurring-detection.test.ts tests/unit/dashboard-screen.test.tsx</automated>
  </verify>
  <done>The dashboard now connects insight widgets to the existing ledger in a predictable, reusable way while preserving a dedicated recurring-detail step.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add supporting dashboard widgets without turning the page into a second ledger</name>
  <files>src/renderer/features/dashboard/LargestTransactionsCard.tsx, src/renderer/features/dashboard/RecentTransactionsPreview.tsx, src/renderer/features/dashboard/DashboardScreen.tsx, tests/unit/dashboard-screen.test.tsx</files>
  <read_first>.planning/phases/06-dashboard-analytics/06-UI-SPEC.md, src/shared/contracts/dashboard.ts</read_first>
  <behavior>
    - Test 1: recent transactions render as a small preview only and support click-through rather than inline editing.
    - Test 2: largest transactions present meaningful high-impact rows and support drill-down without overwhelming the page.
    - Test 3: supporting widgets remain visually secondary to the summary row and main chart area.
  </behavior>
  <action>Implement the recent-transactions preview and largest-transactions widget using the snapshot payload. Keep both widgets compact and readable, with click-through behavior only for recent transactions and context-preserving drill-down for largest transactions. Ensure they support the dashboard hierarchy rather than competing with the top analytics band.</action>
  <acceptance_criteria>Recent transactions remain a preview-only block; largest transactions support drill-down without becoming a second ledger; renderer tests confirm the dashboard hierarchy and widget behavior.</acceptance_criteria>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/dashboard-screen.test.tsx</automated>
  </verify>
  <done>The dashboard now includes the lower-band widgets needed for a complete insight surface without diluting the core analytics hierarchy.</done>
</task>

</tasks>

<verification>
Run the dashboard screen, drill-down, and recurring-detection suites. Confirm drill-down query shaping, recurring-detail flow, and the preview-only behavior of recent transactions.
</verification>

<success_criteria>
Phase 6 dashboard interactions now feel connected and trustworthy: insight widgets can lead into deeper investigation without turning the dashboard into a cluttered transaction browser.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-analytics/06-03-SUMMARY.md`
</output>
